# Air Quality Monitor — ESPHome Secrets Template
# Copy this file to secrets.yaml and fill in actual credentials.
# secrets.yaml is gitignored — never commit it.

wifi_ssid: "JCTnet1"
wifi_password: "<JCTnet1-wifi-password>"

# Fallback AP — used if WiFi connection fails at boot
ap_password: "<choose-a-fallback-ap-password>"

# OTA update password — used for wireless firmware updates
ota_password: "<choose-an-ota-password>"

# MQTT broker — Pi on local network. No hotspot/DuckDNS variant needed:
# home mode only happens while docked/charging at home (Phase 3 decision),
# unlike hiking-monitor which also syncs away from home via cellular hotspot.
mqtt_broker: "pi1.local"
mqtt_username: "air-quality-monitor"
mqtt_password: "<air-quality-monitor-mqtt-password>"
