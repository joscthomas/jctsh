alias: Front Porch Warm - Close Door
mode: single
trigger:
  - platform: numeric_state
    entity_id: sensor.front_porch_temp_sensor_temperature
    above: input_number.front_porch_temp_threshold
    for: "00:10:00"
condition:
  - condition: time
    after: "06:00:00"
    before: "22:00:00"
action:
  - service: notify.mobile_app_pixel_10_pro_xl
    data:
      title: Front Porch Warm
      message: >
        Front porch is {{ states('sensor.front_porch_temp_sensor_temperature') | round(1) }}°F.
        Consider closing the door.
  - service: notify.mobile_app_pixel_7_pro
    data:
      title: Front Porch Warm
      message: >
        Front porch is {{ states('sensor.front_porch_temp_sensor_temperature') | round(1) }}°F.
        Consider closing the door.
