# Template -- copy to .env in this same directory on the M8
# (~/hike-izer-web-app/.env) and fill in the real value. Never commit the
# real .env. See credentials.local.md for the actual generated secret.

# CARD-0086: shared secret the Tasker profile includes as ?key=<value> on
# its webhook POST. The orchestrator rejects any request with a missing or
# incorrect key.
WEBHOOK_SECRET=
