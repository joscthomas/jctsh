# hike-izer — Context

## Card History

**Archived from `tos/kanban-board.md` on 2026-08-22 (CARD-0193)** — 29145B, over the 10000B size threshold.

### CARD-0147 · [idea] [hike-izer] Hike-izer iterative improvement for hike of Aug 10, 2026
**Status:** Done

**Raised 2026-08-10 06:47 MST**, from real observations reviewing today's hike page. Five distinct asks, interviewed same session:

**A. Home link on hike summary pages.** Add a link/button on the hike summary page back to the hikes home/calendar page — currently no way to navigate back without editing the URL by hand.

**B. Map click-to-expand.** Clicking the Route Map opens a full-size modal/lightbox overlay of the same map, for better observability of route/sun-gadget/travel-arrow detail than the normal inline size allows. **Deliberately map-only, no Elevation & Speed chart hover-sync in the expanded view** (Joseph's call, interviewed) — keeping that pairing functional in the modal would either require also carving out modal space for the chart (working against the whole point of maximizing map size) or firing sync events at an on-page chart hidden behind the modal backdrop (invisible, pointless). The modal is a bigger look at the map alone.

**C. "NEW" marker on the Wildlife Heard table.** Mark any species in a hike's own Wildlife Heard table with a small "NEW" badge/pill (Joseph's call on style, interviewed) when that hike is the species' first-ever appearance — i.e. `wildlife_life_list.json`'s `first_heard_file_stem` for that species equals the current hike's own `file_stem`. Distinguishes "first time we've ever heard this bird on any hike" from an established species just showing up again.

**D. Life-list hikes-observed-on count — already exists, no work needed.** Checked directly (Claude, same session) before writing this card rather than assume: `wildlife_life_list.py`'s `update_from_hike()` already maintains a deduplicated `hikes` list per species (idempotent — re-processing the same hike via step 1 then step 2 never double-counts), and `build_wildlife_index.py` already renders `len(e['hikes'])` as a "Hikes" column on `wildlife.html` — distinct from the Wildlife Heard table's own per-hike detection count. This was built as part of CARD-0142 and is already live. Kept as a line item here only for traceability against the original ask, not because anything needs building.

**E. Direction-of-travel arrows (CARD-0085 Part B) — visual rework.** Currently plain rotated markers directly on the route line. Rework to: smaller, lighter stroke weight, and offset to run parallel with the track rather than sitting on top of it (Joseph's call, interviewed: pick a consistent offset rule — e.g. perpendicular to each arrow's own local travel bearing — rather than a single fixed screen-space side; it's fine if the *visual* side varies naturally as the route curves, as long as the rule itself is consistent). This is a refinement of already-built code (`_TRAVEL_ARROW_SVG`/`_select_travel_arrow_points()` in `build_hike_map.py`), not new scope — CARD-0085 itself stays focused on the underlying data pipeline and is unaffected.

**Done when:** all four real work items (A, B, C, E — D needs no action) are built, deployed to the M8, and verified against the live Aug 10, 2026 hike page (or the most recently published hike if that one's already been superseded by further edits).

**Built, deployed, and structurally verified against the real Aug 10, 2026 hike, 2026-08-10 07:20 MST.**
- **A:** `.top-nav` link added to `templating.py`'s hike-summary header (`&larr; All Hikes` &rarr; `index.html`), reusing `build_wildlife_index.py`'s own existing nav-link convention/CSS verbatim rather than inventing a second style.
- **B:** click-to-expand implemented by physically relocating the *same* Leaflet map instance's DOM container into a modal on open (not building/keeping a second map in sync) — a Leaflet control button (`bottomright`, the one open corner: `topright` is the sun gadget, `topleft` is Leaflet's own zoom control) triggers it. `invalidateSize()` + re-`fitBounds()` on both open and close, since Leaflet doesn't notice a container move/resize on its own and the aspect ratio genuinely changes between the small inline card and the large modal.
- **C:** "NEW" badge added to `birdnet_table_rows()`/the Wildlife Heard row markup. **Real bug found and fixed during verification, not just built clean:** the first implementation checked "is this species absent from the life list" -- correct logic for a hike's *first-ever* render (confirmed `render_html()` always runs before `wildlife_life_list.update_from_hike()` in `generation.py`'s own pipeline), but breaks on any *regeneration* of an already-processed hike, which this project does routinely (CARD-0140/144/0085 all did it) -- by the time of a regen, that hike's own species are already merged into the life list, so every one of them would wrongly read "already known" forever after, silently defeating the whole feature on exactly the kind of re-render this session was already doing. Fixed by comparing `first_heard_file_stem` to the current hike's own `file_stem` instead -- a stable, idempotent fact recorded once, correct regardless of how many times a page is rebuilt (matches the card's own original interview wording, which was right the first time).
- **E:** stroke-width 2.5 -> 1.75, icon 18px -> 14px, added `opacity: 0.85`, lighter drop-shadow. Perpendicular offset achieved via CSS transform order alone (`rotate(bearing) translateX(9px)`, translate applied first/innermost) -- no real lat/lon geo-math needed: the pre-rotation local-space translate lands at `bearing+90deg` in real/screen space once the rotation carries it along, i.e. consistently perpendicular to each arrow's own travel direction, "beside the track" rather than on it, varying in absolute compass terms exactly as the route curves (Joseph's own stated tolerance).

**Verification used today's real hike as the test case throughout, not synthetic data** -- confirmed live against `2026-08-10_hike-summary.html` (regenerated in place, reusing the already-fetched photos manifest and re-parsing the still-present staged BirdNET zip, no new Immich/Claude API cost): home link present and `index.html` target reachable (200); modal/expand-control markup present; arrow CSS/offset values present (`stroke-width="1.75"`, `iconSize: [14, 14]`, `translateX(9px)`); and for C specifically, cross-checked the life list directly and found two species genuinely first-heard today (Northern Flicker, Green Frog -- both `first_heard_file_stem: 2026-08-10`, `hikes` count 1) versus 8 others from earlier hikes -- the regenerated page's NEW badge landed on exactly those two rows and no others, a real positive/negative test rather than just confirming the markup exists. **Not yet visually confirmed in a live browser** (the automated browser tool has been unreliable this session, same recurring caveat as CARD-0085's own gadget work) -- worth Joseph's own look, especially for B's modal open/close feel and E's arrow legibility at real map zoom, before calling this fully done.

**Two real bugs found live in B (modal), fixed 2026-08-10 07:29 MST -- exactly the kind of thing structural verification alone can't catch, only an actual look could:**
- **"Map expanded but the route didn't"** -- the modal box grew, but the map/route rendering inside stayed the same size, floating in a bigger empty container. Root cause: `invalidateSize()`/`fitBounds()` were called via a bare `setTimeout(fn, 0)` after the DOM move + CSS display change, which doesn't reliably guarantee the browser has actually finished layout on the just-shown, just-resized container by the time it fires -- Leaflet ended up computing against a stale size. Fixed with a forced synchronous reflow instead (`void container.offsetHeight` immediately after the move/display change, before calling refit()) -- reading a layout property forces the browser to compute layout right then, a standard, reliable technique for this exact class of bug, rather than hoping a timeout won this race.
- **Blank gap in the bottom half of the modal.** Root cause: the map's fill-height chain (`.map-modal` -> `.map-modal-container` -> the moved `.hike-map` div) relied on `height: 100%` cascading through several nested divs -- fragile and, in this case, wrong somewhere in the chain (not fully traced by inspection alone). Replaced with an explicit flexbox fill pattern (`.map-modal` as `display:flex; flex-direction:column`, `.map-modal-container` as `flex: 1 1 auto; min-height: 0`) -- a more robust, harder-to-get-wrong way to say "fill the remaining space," and sidesteps percentage-height resolution edge cases entirely. `min-height: 0` matters here specifically: flex items default to `min-height: auto`, which can stop a child with its own internal sizing (a Leaflet map) from actually shrinking/growing to fill a flex parent -- a well-known flexbox gotcha.

Both fixes deployed and re-verified against a fresh regeneration of the same real `2026-08-10_hike-summary.html` -- forced-reflow JS and flex CSS both confirmed present in the output, NEW badge still correctly on exactly the same 2 rows (no regression from the unrelated CSS/JS changes). **Still not visually confirmed in a live browser** -- these fixes are reasoned from the actual bug mechanics (confirmed real Leaflet/flexbox gotchas, not guesses) and structurally verified, but the modal's actual on-screen feel remains unconfirmed until Joseph looks again.

**First real screenshot, 2026-08-10 07:37 MST -- confirms both modal fixes above actually worked** (route shows real street-level zoom, no floating-in-empty-space; modal box ends right after the Leaflet attribution line, no visible blank gap). Surfaced a separate, genuine issue instead: event markers (photo/observation/bird-sighting circles, `.map-marker-icon`) read too large and overlap heavily where several land close together on the route -- a real cluster of bird-sighting markers in the screenshot was nearly indistinguishable as separate items. Also asked whether the white circle background is even needed. **Recommendation given and applied:** keep the circle -- these are thin stroke-only line-art icons (not filled shapes) against Thunderforest's visually busy "Outdoors" basemap (roads, labels, green space), and dropping the background would likely cost real legibility -- but shrink everything. `.map-marker-icon` 26px -> 20px (inner SVG 16px -> 12px, border 1.5px -> 1.25px, lighter shadow), matching `L.divIcon`'s own `iconSize`/`iconAnchor` in `build_hike_map.py`. Deployed, regenerated against the same real hike page, confirmed present in the output. Not yet re-screenshotted to confirm the overlap actually reads better at the new size.

**Correction to the "no visible blank gap" claim above, 2026-08-10 07:50 MST -- it was wrong.** Joseph's own laptop screenshot tool couldn't capture the modal overlay at all (only whatever was behind it), so he took a real phone photo of the screen instead -- which showed a real, still-present blank gap below the map, plus asked to "expand the map vertically so it fills the modal box." **Real root cause, found by actually reading the competing selectors rather than re-guessing:** the modal markup is nested *inside* the Route Map's own `<section>` (inside `.hike-visuals`, since `build_map_html()`'s return value -- inline card + modal together -- gets embedded as one block by `render_html()`) -- so even after JS relocates the map div into the modal, it's still a genuine DOM descendant of `.hike-visuals`, `position: fixed` notwithstanding (CSS descendant selectors match by DOM ancestry, not visual/layout escape -- the exact wrong assumption the earlier "no gap" claim rested on). `.hike-visuals .hike-map`'s own responsive rule (`height: 15rem`, active at typical laptop widths) has the *same* specificity as the plain 2-class selector this fix used, and is declared later in the stylesheet -- so it silently won the tie and clamped the map to 15rem regardless of the modal's real size. Fixed with a 3-class selector (`.map-modal-backdrop .map-modal-container .hike-map`), unambiguously higher specificity, so it wins regardless of source order or which other responsive rule also matches.

**Marker overlap -- both raised options from the interview applied together (Joseph: "1 and 2"), not either/or:**
1. Shrunk further, 20px -> 16px (inner SVG 12px -> 9px, border 1.25px -> 1px) -- same size/CSS-only direction as the first pass.
2. **New: server-side marker declutter/spread** (`templating._declutter_markers()`), since the remaining overlap wasn't purely a size problem -- several BirdNET detections logged seconds apart while stationary land at genuinely near-identical GPS positions, which no amount of shrinking alone would separate. Groups markers within `MARKER_CLUSTER_RADIUS_M` (8m real-world distance, haversine -- not a fixed lat/lon epsilon, which would be wrong at different latitudes since longitude degrees shrink toward the poles) and spreads each group evenly around its own centroid at `MARKER_SPREAD_RADIUS_M` (5m) -- a simple deterministic version of the "spiderfy" technique clustering map libraries use, computed in plain Python (no new JS dependency) since every marker's real lat/lon is already known server-side at render time.

All three fixes deployed and verified against a fresh regeneration of the same real `2026-08-10_hike-summary.html`: the higher-specificity selector, 16px markers, and `iconSize:[16,16]` all confirmed present in the output; NEW-species badge still correct (no regression). Decluttering verified by computing real pairwise distances between every marker in the final output -- no two markers closer than ~8.5m (the cluster threshold), and the three pairs that WERE clustered now sit ~10-12m apart, matching almost exactly what two points spread to opposite sides of a 5m-radius circle around a shared centroid should produce -- a real, checked result, not just "the code looks right."

**Real photo of the declutter fix showed the overlap barely changed, 2026-08-10 08:05 MST -- root cause was the metric itself, not the math.** The 8m/5m real-world-meter thresholds were correctly computed (per the pairwise-distance check above), but real-world meters translate to a *fixed* pixel distance only at a given zoom level -- the same 10-12m separation that looked adequate at the modal's high zoom was still visually cramped at the inline card's lower zoom, since more real-world meters fit into the same pixel space when zoomed out. A server-side, meters-based fix can't be zoom-aware, because rendering happens once at generation time while zoom happens live in the browser. Joseph proposed the fix directly: move markers off the route into open map space with a leader line back to the real point on hover, which decouples "far enough apart to read" from "real-world distance" entirely -- offered a choice between that and a separate strip/legend layout; Joseph picked "callout pins in open map space."

**Rebuilt as a client-side pixel-space callout system, 2026-08-10 08:15 MST -- `_declutter_markers()`/`_haversine_m()` removed from `templating.py` entirely, replaced by `layoutEventMarkers()` in `build_hike_map.py`.** Runs in the browser on `zoomend`/`moveend`, so it's correct at every zoom level, not just the one assumed at render time:
- Each marker's real lat/lon is projected to its current on-screen pixel position (`latLngToContainerPoint`), then offset perpendicular to the route's local bearing at that point (found by nearest-track-point lookup, same perpendicular-via-CSS-transform trick as the E travel arrows) by `MARKER_OFFSET_PX` (22px).
- If that offset position is still within `MARKER_CLUSTER_PX` (18px) of an already-placed marker, it steps further out along the same perpendicular direction in `MARKER_SPREAD_STEP_PX` (16px) increments (up to 8 tries) until clear -- markers can now spread as far as needed since they're no longer constrained to sit near the real point.
- On hover, a dashed leader line (`marker-leader-line`) and a small highlight dot (`marker-real-point`) are drawn from the callout back to the actual real-world point on the track, added/removed live so they don't clutter the resting state.
- Also addressed in the same round: continuous zoom (`zoomSnap: 0.25` on the Leaflet map init, replacing the default fixed integer zoom levels), per Joseph's question "how about a zoom that is continuous rather than a set zoom value?"; and the sun-direction gadget circle restyled smaller and to actually resemble a sun (2.75rem -> 2.1rem, `var(--surface)` flat fill -> gold `radial-gradient` with amber border/glow, arrow color `var(--accent)` -> dark amber `#6b4416`) per the mid-round ask "make the circle smaller and resemble a sun."

Verified structurally against a fresh regeneration of the same real `2026-08-10_hike-summary.html`: `zoomSnap: 0.25` present; `layoutEventMarkers`/`MARKER_OFFSET_PX`/`marker-leader-line` all present (9 occurrences); restyled `.sun-gadget` CSS (radial-gradient, amber border) present; NEW-species badge still correct on exactly the same 2 rows (no regression); page live, HTTP 200. Also confirmed via a standalone script generating real HTML with test chart/marker data and checking brace/paren/bracket balance in the embedded `<script>` block (all balanced) plus manually reading the generated `layoutEventMarkers()` output for logic correctness, since `py_compile` alone only validates the Python side, not the JS it's templating. **Not yet visually confirmed in a live browser or real photo** -- same recurring caveat as the rest of this card's work; worth Joseph's own look at the callout/leader-line behavior, continuous zoom feel, and sun gadget appearance before this round is called done.

**Two more real issues found on Joseph's own look, fixed 2026-08-10 08:30 MST:**
- **Leader line obscured by its own tooltip.** The callout hover tooltip was fixed at `direction: "top"`, but the leader line runs from the callout back toward the real point in whatever direction that happens to be -- "almost all cases" it ran roughly upward too, so the tooltip box sat right on top of it. Fixed by pointing the tooltip along the *same* `dir` vector already used to place the callout (away from the route) instead of a fixed side -- since the leader line runs the opposite way (`-dir`, back toward the real point), the tooltip is now guaranteed clear of its own line regardless of which way the route curves at that spot, the same "derive it from the actual geometry, not a fixed assumption" fix pattern as several of this card's earlier rounds.
- **Sun gadget restyled to actually look like a sun**, per a reference image Joseph linked (a classic icon: gold circle plus radiating pointed rays), not just a plain warm circle. Added an 8-point sunburst (`.sun-gadget-rays`, orange `#f7941e`, points computed offline into a static polygon, not live math) behind the existing gold circle. The layering is done via DOM sibling order, not z-index -- a child element's own background can never paint behind its parent's, so the circle moved from being `.sun-gadget`'s own background to a second sibling (`.sun-gadget-core`) painted after the rays, letting the spike tips show past its edge while the opaque circle covers the rest of the burst.

Both deployed and verified against a fresh regeneration: `tooltipDir` logic and the new `.sun-gadget-rays`/`.sun-gadget-core` structure both confirmed present; NEW-species badge still correct (no regression); page live, HTTP 200.

**A third real issue, fixed 2026-08-10 08:45 MST -- travel arrows weren't reliably parallel to the drawn track.** The arrow's rotation had been using each point's stored `travel_bearing_deg` (a real GPS-reported heading) ever since CARD-0085 built it. Joseph's explicit requirement -- "should always be parallel to the track, but not touch the track" -- is a promise about what's *drawn* on the map, and a device's instantaneous heading can genuinely diverge from the straight segment between two plotted points (sensor noise, a brief pause, a tight switchback between samples), which no amount of styling would fix since the two numbers just don't always agree. Fixed by deriving the rotation from the route polyline's own local on-screen tangent instead (`tangentAngleDeg()` in `build_hike_map.py`, reading straight from `points[]` via `latLngToContainerPoint` -- the exact same data and technique the event-marker callouts already use for their own perpendicular offset), so the arrow is now geometrically guaranteed to match what's actually drawn, not just usually close to it. `travel_bearing_deg` is kept only as the *gate* for which points get an arrow at all (real GPS-reported heading confirms it's a genuine waypoint) -- its numeric value is no longer used for the angle. The existing perpendicular-offset mechanism (CSS `rotate() translateX(9px)`, unchanged) still keeps the arrow off the line itself, and is now built on the same guaranteed-accurate angle.

Deployed and verified against a fresh regeneration: `tangentAngleDeg` defined and wired to each arrow (`tangentAngleDeg(t.idx)`), the old `t.bearing`-based rotation fully removed from the output, NEW-species badge still correct, page live, HTTP 200.

**Closed out 2026-08-10 08:50 MST on Joseph's go-ahead.** Every item this card tracked (A/B/C/E, D needing no action) has been built, deployed, and iterated against real feedback from today's actual hike across several rounds -- including catching and fixing real bugs (modal route-scaling, modal blank gap, marker overlap being fundamentally zoom-dependent, tooltip-over-leader-line, and this final travel-arrow angle-source bug) that structural verification alone couldn't have surfaced, only Joseph's own screenshots/photos and explicit requirements could. The last two rounds (leader-line/sun gadget, travel-arrow angle) are structurally verified the same way as everything else on this card but not yet re-confirmed in a fresh screenshot -- if either doesn't look right live, it reopens under a fresh card rather than this one, per the project's own convention for closed work.

**Reopened 2026-08-10 09:05 MST -- F. Sortable Wildlife Life List columns.** Joseph's call to fold this into this card rather than open a new one, since it's a small, self-contained addition to the same page family (`wildlife.html`, `build_wildlife_index.py`) this card's other items already touched. Click any of the four column headers (Common Name, Scientific Name, First Heard, Hikes) to sort ascending/descending, with a small triangle indicator on the active column. `build_wildlife_index.py` was explicitly "deliberately zero-JS" per its own docstring (modeled on `build_calendar_index.py`) -- this is a scoped, documented exception, since sorting genuinely needs client-side interactivity that no static render can provide. Implementation: each `<td>` carries a `data-sort-value` (lowercased for the two text columns, the raw `YYYY-MM-DD` `first_heard_file_stem` for First Heard -- already lexicographically sortable as-is, no separate date parsing needed -- and the raw integer for Hikes), each `<th>` carries a `data-sort-type` ("text" or "number") so the click handler knows whether to `localeCompare` or subtract; clicking the same column again reverses direction. Deployed and verified against a fresh regeneration of the real `wildlife.html` (33 species): 132 `data-sort-value` attributes present (33 x 4 columns), all 4 `data-sort-type` headers present, script brace-balanced, page live (HTTP 200). **Not yet visually confirmed** -- worth Joseph's own click-through before this closes again.

**G. Same two features on the per-hike Wildlife Heard (BirdNET) table, plus a Life List link in its header** -- Joseph's follow-up while F was landing. Brings the per-hike table in line with the cross-hike life-list page:
- Wikipedia link on each species' common name, reusing `build_wildlife_index.wikipedia_url()` directly (renamed from `_wikipedia_url` -- no longer private, now a cross-module helper) rather than a second copy of the same URL-building one-liner. `templating.py` already imports `build_hike_map`/`build_hike_chart` as sibling modules the same way (documented in its own header comment), so importing `build_wildlife_index` too follows an established pattern, not a new one.
- Click-to-sort on all four columns (Species, Count, Confidence, Time), scoped specifically to `#birdnet-table` -- the hiking-observations table elsewhere on the same page also uses the shared `.obs-table` class but isn't sortable, so both the CSS (cursor/hover/arrow) and the sort script target the id, not the class, to avoid implying every table on the page is clickable. Confidence sorts on the raw percent int (`confidence_pct`), not the displayed `"83%"` string; Time sorts on the raw ISO timestamp (`time_iso`), not the displayed `"8:01 AM"` string, since AM/PM text doesn't order correctly around noon. Unlike F's script, this one starts with no column marked active (`state.col = -1`) rather than assuming column 0 -- the table's real initial order is birdnet.py's own first-detection-time grouping, not a simple single-column sort, so claiming Species was "already sorted ascending" would have been a false default.
- `<h2>Wildlife Heard (BirdNET)</h2>` wrapped in a new `.section-header` flex row with a "Life List &rarr;" link to `wildlife.html` -- a lighter-weight inline pill than the page-top `.top-nav` link (A), sized to sit next to a section heading rather than at the very top of the page.

Deployed and verified against a fresh regeneration of the real `2026-08-10_hike-summary.html`: `#birdnet-table` present once, 40 `data-sort-value` attributes (10 species x 4 columns), 10 Wikipedia links, the Life List header link present, NEW-species badge still correct (no regression from the unrelated table changes), all 3 embedded `<script>` blocks on the page brace-balanced, page live (HTTP 200) -- and `wildlife.html` re-verified in the same pass to confirm the `wikipedia_url` rename didn't break its own existing link (33 links, 132 sort attributes, still live). **Not yet visually confirmed** -- same caveat as F.

**Two real bugs found on Joseph's first actual look (screenshot), fixed 2026-08-10 09:20 MST:**
- **Garbled characters in the sort-arrow column headers** (`SCIENTIFIC NAME□9□B2` in the screenshot). Root cause: the sort-arrow CSS (`content: "\2009\25B2"`, meant as CSS's own hex-escape syntax for a thin space + a "▲" triangle) was written inside a *plain* Python string, not a raw one -- Python parsed `\2009`/`\25B2` as its own octal escapes (`\200` = byte 0x80, `\25` = byte 0x15) before the CSS ever reached the browser, silently corrupting the value into control bytes plus leftover literal digits. This existed in both `build_wildlife_index.py` (life list) and `templating.py` (per-hike table) -- same bug, same copy-paste origin, Joseph flagged the hike page had it too before a second screenshot was even needed. Fixed by writing the actual Unicode characters directly in the Python source (thin space + ▲/▼) instead of a CSS-escape string, sidestepping the Python-vs-CSS escape-syntax collision entirely rather than just correcting the escape (e.g. to `\\2009\\25B2`) and leaving the same trap for the next person who copies this pattern.
- **Wildlife names didn't read as links.** `wildlife.html`'s own `td a { color: var(--accent); text-decoration: none; }` styled every link in its table (including the newly-added Wikipedia links) as green, no underline -- a deliberate original design choice for that page, but one Joseph now wants reverted to look like an actual hyperlink, "the standard blue underline... like on the hike page." The hike page has no such override for its plain `<a>` tags (confirmed -- no general `a`/`td a` rule in `templating.py`'s stylesheet), so its own Wikipedia links already render in the browser's default blue/underline; removed the override in `build_wildlife_index.py` entirely so `wildlife.html` now matches that same unstyled-default look rather than adding a second, separate blue/underline rule to maintain.

Both fixes deployed and verified against a fresh regeneration of both real pages: no corrupted 0x80/0x15 bytes in either page's output, `td a` color override confirmed removed from `wildlife.html`, both pages live (HTTP 200).

**Closed out again 2026-08-10 09:25 MST -- Joseph confirmed live.** Every item across the full card (A/B/C/E/F/G, D needing no action) is built, deployed, and now visually confirmed working, including this round's two real bugs (garbled sort-arrow header text, wildlife-name link styling) that only Joseph's own screenshot caught.

**Related:** CARD-0082 (Route Map this extends), CARD-0110 (Elevation & Speed chart, the hover-sync pairing B deliberately excludes from the expanded view), CARD-0085 (travel-direction arrows E reworks the visuals of, not the underlying data), CARD-0142/CARD-0143 (wildlife life list D found already-built), `components/hike-izer/build_hike_map.py`, `components/hike-izer/build_wildlife_index.py`, `components/hike-izer-orchestrator/wildlife_life_list.py`, `components/hike-izer-orchestrator/templating.py`.

---

**Archived from `tos/kanban-board.md` on 2026-08-22 (CARD-0193)** — 12781B, over the 10000B size threshold.

### CARD-0133 · [idea] [hike-izer] Route Map event markers — photos, hike observations, bird sightings
**Status:** Done

**Raised 2026-08-01**, split out of CARD-0082 (Done) — CARD-0082's own Planning notes always scoped this as a later phase ("Design the marker system generically/extensibly rather than hardcoding a fixed list of event types — whatever ends up timestamped and geotagged in the pipeline should be markerable, not just the types known today"), not part of that card's Done criteria. This card owns building it.

**Real status of each candidate marker type, checked against the actual data, not assumed:**
- **Photos (CARD-0084, Done)** — genuinely markerable today. `fetch_hike_photos.py`'s manifest carries real per-asset `lat`/`lon` from Immich EXIF (confirmed live, 2026-08-01: every asset in a real manifest had its own coordinates, not a session-level average).
- **Hiking Observations** — **corrected 2026-08-02, live-checked against the real "Hiking Observations" sheet:** no interpolation needed at all going forward. `environmental-data.gs`'s `_gpsLookup()` already populates real `lat`/`lon` on every observation row at write time (nearest GPS-track point within ±5 min) — confirmed against 40 real rows: every one from 2026-07-28 onward carries real coordinates, every earlier one has `lat: ""`, `lon: ""`. Built to just read the field directly and skip the marker (same "not available, omit" convention used everywhere else) when it's missing — historical pre-2026-07-28 observations simply won't get a marker unless someone asks for a backfill later.
- **Bird sightings (CARD-0080, Done, BirdNET)** — **corrected 2026-08-01: genuinely markerable per-sighting, not just per-session.** Re-checked `birdnet.py` directly: each raw `detections[]` entry has its own precise per-detection UTC `timestamp` (confirmed against two real exports, per that file's own docstring) — GPS is session-level only, but *time* is per-detection. The earlier "one approximate marker per session" read came from `parse_detections()` (lines 76-99) immediately collapsing every detection down to one row per species and discarding all but the earliest timestamp — an aggregation choice already baked into today's table view, not a real limit of the underlying data. Same track-timestamp-interpolation approach as Hiking Observations works here too, once `parse_detections()` stops discarding per-detection timestamps.

**Real architectural gap, found while scoping this — resolved by CARD-0134 (Done, 2026-08-01):** at the time this card was raised, CARD-0082's Route Map and CARD-0110's Elevation & Speed chart existed only in `components/hike-izer/`'s interactive-Skill toolkit, not in the real automatic pipeline (`hike-izer-orchestrator`) that actually builds the live pages at `hikes.jctnet.com`. CARD-0134 wired both into `templating.py`/`generation.py`, verified against real data — so this card's markers now have a real target pipeline to build against, no longer an open question.

**Design intent carried forward from CARD-0082:** generic/extensible marker mechanism, not a hardcoded list of three types — whatever ends up timestamped (and geotaggable, directly or by interpolation) in the pipeline should be markerable without a code change per new event type.

**Visual design direction, settled 2026-08-01 (Joseph's steer, not yet built):**
- **Marker shape: icon glyphs, not dots.** `L.divIcon` with inline SVG (vendored, no external icon CDN, same convention as everything else on this map) — camera for Photos, cartoon talking-bubble for Observations, bird for bird sightings. Colored per type using the same CSS-token pattern the chart already uses (`--chart-elevation`/`--chart-speed`-style tokens). Unknown/future event types fall back to a plain colored dot, keeping the "generic, not hardcoded types" intent from CARD-0082 intact.
- **Clustering: zoom-based separation, no plugin.** The map already has zoom controls (only `scrollWheelZoom` is disabled) — markers at real distinct coordinates naturally spread apart as you zoom in, zero extra code, no clustering plugin needed (which would've broken the "vendored Leaflet only, no plugins" convention). This works for Photos and Observations directly, and now for Birds too once per-detection interpolation lands (see below) — corrected from an earlier read where birds looked like they'd all stack at one identical session-level point.
- **Photos:** hover shows the asset's existing `thumb` file (already downloaded locally by `fetch_hike_photos.py`, same file the Photos gallery already uses — no new fetching); click opens the existing `original` file, reusing whatever "view full size" mechanism the gallery section already has rather than inventing a second one.
- **Observations:** hover tooltip shows Time / Observation text / Categories — identical fields to the existing Full Observations Log table. Needs the GPS-track time interpolation already noted above (unbuilt, no blocker).
- **Bird sightings:** hover tooltip shows species (common + scientific name) / confidence / detection count — same fields as the existing BirdNET table. Requires two changes to `birdnet.py`: (1) `parse_detections()` stops discarding per-detection timestamps down to one `first_timestamp` per species, and (2) consecutive same-species detections within a short time gap get grouped into one "occurrence" (same repeated-reading-collapsing idea used elsewhere in this project) rather than one marker per raw detection — BirdNET Live can re-trigger every few seconds on a bird that never moved, so one marker per raw ping would clutter the map for no reason. Each occurrence gets its own track-time-interpolated position, same mechanism as Observations.
- **Position-precision caveat (still open, not yet decided):** Photos are exact (real per-asset EXIF GPS); Observations and bird occurrences are interpolated against the GPS track, not exact fixes. Worth a lightweight "(approximate location)" note in those two tooltip types rather than a full visual precision system — doesn't need solid/hollow marker styling, just a line of text, so as not to overload an already-busy icon+color+tooltip design.

**Built 2026-08-02, grounded in a real implementation plan (three Explore-agent research passes over `generation.py`/`templating.py`/`build_hike_map.py`/`fetch_hike_photos.py`, plus a live query of the real "Hiking Observations" sheet):**
- **`components/hike-izer/build_hike_map.py`** — new `interpolate_position(chart_series, target_iso_ts)`: session-safe (groups by `session_break`, never bridges a multi-hour gap between two sessions), linearly interpolates between bracketing points when the target falls inside a session, clamps to the nearest endpoint when just outside one (within `MAX_INTERPOLATION_GAP_SEC` = 600s, matching `generation.py`'s own `SESSION_QUERY_PADDING`), returns `None` otherwise. No existing bracket-and-interpolate helper existed anywhere in the repo to reuse — genuinely new. `build_map_html()` gained a `markers=None` param: generic `{type, lat, lon, tooltip_html, click_url}` list, rendered as `L.divIcon` (inline SVG camera/speech-bubble/bird, `_MARKER_ICONS` lookup falling back to a plain dot for unknown types — keeps CARD-0082's "generic, not hardcoded types" intent real, not just documented), `.bindTooltip()` for hover, click opens `click_url` in a new tab for photos. Entirely separate from the existing chart-hover-sync circleMarkers — no interaction with those CustomEvents.
- **`components/hike-izer-orchestrator/birdnet.py`** — refactored so `parse_detections()` (existing species-aggregated table, **output shape unchanged**, zero regression risk to CARD-0080's already-shipped table) and new `parse_occurrences(staging_dir, gap_minutes=5)` share one internal raw-detection loader instead of parsing the staged export files twice. `parse_occurrences()` groups consecutive same-species detections within `gap_minutes` into one "occurrence" (avoids one marker per raw BirdNET re-trigger on a bird that never moved), each with a `representative_timestamp` (the occurrence's own start/end midpoint) for `interpolate_position()` to place. Unit-tested against synthetic BirdNET-shaped detections: confirmed a 3-detection burst collapses to one occurrence, a detection 10 minutes later correctly splits into a second one, and `parse_detections()`'s existing output is byte-for-byte unchanged.
- **`components/hike-izer-orchestrator/templating.py`** — new `_build_event_markers()`: Photos use `fetch_hike_photos.py`'s existing per-asset EXIF `lat`/`lon` (skipped when `None`), tooltip reuses the existing `thumb` file, click reuses the existing `original` file — no new file handling. Observations read `lat`/`lon` directly per the correction above — no interpolation call for this type. Birds call `build_hike_map.interpolate_position()` per occurrence, skipped when it returns `None`. Observation/bird tooltips get a plain "(approximate location)" text note rather than a separate visual precision system. `render_html()` gained `birdnet_occurrences=None`; `photos_dir` is now computed once near the top of the function (markers need it before the existing Photos section does). New CSS tokens `--marker-photo`/`--marker-observation`/`--marker-bird` (purple/teal/amber — chosen for separation from `--accent`/`--chart-elevation`/`--chart-speed`/`--warning`/`--danger`) plus `.map-marker-icon`/`.leaflet-tooltip` rules (Leaflet's own tooltip box+arrow restyled to match the page instead of its default white-box look).
- **`components/hike-izer-orchestrator/generation.py`** — `run_step2()` now also calls `birdnet.parse_occurrences()` and passes `birdnet_occurrences` into `render_html()`. Bird markers only appear once step 2 runs, same as the existing BirdNET table (step 1 has no staged export to read yet).
- **`components/hike-izer/html-template.html`** — same new CSS tokens/rules ported verbatim into the interactive Skill's own reference template, matching CARD-0134's established "ported field-by-field" convention.
- All five files pass `python -m py_compile`.

**Verified live on the M8, 2026-08-02** (unit-level first, then full end-to-end against real data, all output to scratch paths only — no published page touched, all scratch files deleted after):
- `interpolate_position()` unit-verified against a real 63-point `chart_series` (mid-session interpolation lands correctly between its bracketing points; a timestamp just outside the session clamps to that session's own endpoint) and against a synthetic two-session series specifically for the boundary case — confirmed a timestamp in the middle of a 4-hour inter-session gap returns `None` rather than bridging across it, and timestamps just outside either session clamp to *that* session's own endpoint, never the other one's.
- `parse_occurrences()` unit-verified against synthetic BirdNET-shaped detections: a 3-detection burst collapsed to one occurrence, a detection 10 minutes later split into a second one, `parse_detections()`'s existing table output unchanged.
- Deployed all four updated files to the M8, rebuilt the orchestrator container, confirmed `Up ... (healthy)`.
- Full `render_html()` run inside the container against a real fresh hike fetch (2026-07-30) + real Immich photos for that same window + two real observation rows spliced in from the actual 2026-07-28 sheet data (plus one deliberately-included pre-2026-07-28 row with no coordinates, to confirm it's skipped) + a synthetic BirdNET export (no real staged export existed to test against). Extracted the actual `eventMarkers` JS payload baked into the output and confirmed exactly: **7 photo markers** (all 7 real Immich assets, real `thumb`/`original` filenames in the tooltip/click URL), **2 observation markers** (both real post-2026-07-28 rows; the pre-2026-07-28 row correctly produced no marker), **1 bird marker** (2 synthetic detections correctly grouped into 1 occurrence, and its interpolated position matched the real `chart_series` point at that exact timestamp exactly, lat/lon to 6 decimal places).

**Related:** CARD-0082 (Done — this card's origin, the Route Map this adds markers to), CARD-0110 (Done — sibling chart, same `chart_series` data source question applies), CARD-0084 (Photos, Done — the one marker type with real per-event GPS today), CARD-0080 (Bird ID, Done — supplied the raw per-detection timestamps `parse_occurrences()` groups), CARD-0134 (Done — the orchestrator integration this card's markers are built against), `components/hike-izer/build_hike_map.py`, `components/hike-izer-orchestrator/birdnet.py`, `components/hike-izer-orchestrator/templating.py`, `components/hike-izer-orchestrator/generation.py`.

---

**Archived from `tos/kanban-board.md` on 2026-08-22 (CARD-0193)** — 22500B, over the 10000B size threshold.

### CARD-0110 · [idea] [hike-izer] Hiking stats — elevation graph, elevation summary, speed graph, other stats
**Status:** Done

**Raised 2026-07-28**, distinct from CARD-0082 (route map + elevation profile, Gaia-GPS-style): this card is about the numeric/chart stats layer — no basemap, no route visualization, purely derived from data hike-izer's GPS Track already has.

**Reference material — Joseph's own Gaia GPS data-summary screenshot for today's hike:**
- A combined **Speed + Elevation chart**, both series plotted on one graph against Distance on the X-axis (Gaia also offers Time as an X-axis option) — Speed in one color, Elevation in another, distinct Y-axes (mph / ft).
- A **stats block** richer than hike-izer's current Duration + Elevation Gain: **Distance** (1.27 mi), **Moving Time** (29m 47s) vs **Total Time** (32m 23s) vs **Stopped Time** (2m 36s) as three distinct values — not just one duration, **Pace** (25:27 min/mi), **Moving Speed** (2.6 mph) vs **Avg Speed** (2.3 mph) as distinct values, and **Ascent** (49 ft) / **Descent** (40 ft) as separate numbers — richer than hike-izer's current single net "Elevation Gain."

**Scope:**
1. Elevation + Speed chart, Distance on the X-axis, interactive hover (see Implementation plan below — resolved 2026-08-01, supersedes the original "distance or time" framing).
2. Elevation summary stats — Ascent/Descent as separate values, replacing/supplementing the current single Elevation Gain figure (CARD-0109 already removed the old Elevation Range row from Data Summary as prep for this).
3. Speed graph — new; hike-izer has no speed visualization today.
4. Other stats — Moving Time/Stopped Time/Total Time as distinct values, Moving Speed/Avg Speed as distinct values, Pace.

**Build-vs-embed decision resolved 2026-08-01 — native render is the only option.** CARD-0104's embed precedent (Gaia's map/track widget) doesn't extend here: confirmed there's no equivalent embeddable widget on the Gaia GPS website for the stats/chart view (elevation+speed chart, the Distance/Moving-Stopped-Time/Pace/Ascent-Descent block) the way there is for the route map. The reference screenshot is Gaia's own app UI, not something Gaia exposes for embedding elsewhere. So this card's Scope (below) has to be built server-side from `fetch_hike_data.py`'s `gps_rows`.

**Implementation plan, Planning stage 2026-08-01 — grounded in the real code, not assumed:**

**Existing plumbing this can reuse directly** (`components/hike-izer/fetch_hike_data.py`, re-read in full):
- `_haversine_m(lat1, lon1, lat2, lon2)` — great-circle distance between two points, already used for both session distance summing and per-interval speed.
- Per-interval speed is already computed internally (`_haversine_m(...) / dt`) for hike classification (`_classify_hike`, `_truncate_trailing_fast_activity`) — same pattern extends directly to a full per-point speed series, just not currently exposed as output.
- `WALKING_SPEED_MIN_MPS = 0.15` — already the project's own tuned "effectively stationary" cutoff; reused below for moving-vs-stopped classification rather than inventing a second threshold.
- GPS points are already scoped to just the confirmed hike session(s) via `_rows_in_hike_sessions()` / `coverage['gps_track']['sessions']` — the same scoping `stats['altitude_ft']` and `stats['distance_mi']` already use, so the new stats stay consistent with figures already on the page.
- `.stat-row` / `.stat` / `.stat__label` / `.stat__value` CSS in `html-template.html` already supports an auto-fill grid variant (used for the 5-card Weather Forecast section) — the right pattern for the new, larger stat block (7 new values won't fit the existing 4-card hero row).

**Design decisions resolved this session:**
1. **Altitude noise filtering — threshold filter.** Raw consecutive-point altitude deltas will overcount ascent/descent from GPS jitter alone. Only count a delta toward ascent/descent if `abs(delta_ft)` exceeds a small threshold (proposed default: 5 ft, tunable like the file's other constants, e.g. `WALKING_SPEED_MIN_MPS`) — sum the deltas that clear the threshold, discard the rest as noise. No smoothing pass; simpler, stdlib-only, one constant to tune later if it's over/under-filtering on a real hike.
2. **Moving vs. stopped time — reuse the existing speed threshold.** Classify each point-to-point interval as moving or stopped using the same `WALKING_SPEED_MIN_MPS` cutoff already established for whole-session classification, then sum interval durations into `moving_time_min` / `stopped_time_min`. `total_time_min` is just session `duration_minutes` (already computed) — no new logic needed there.
3. **Chart design — matches Gaia's real behavior, not a static image.** X-axis: Distance. Two line series sharing the plot: **Elevation** (left Y-axis, ft) and **Speed** (right Y-axis, mph). Interactive hover: hovering a point on either line shows that line's value (elevation or speed) plus the time-of-day at that point, displayed along the top of the chart — matching Gaia's own hover UX exactly, per Joseph's description. This is real interactivity, not a static picture, so it does **not** stay zero-JS the way the rest of hike-izer's template does — correcting the initial framing from earlier in this card's history.
4. **Rendering approach — hand-rolled SVG + a small vanilla-JS hover handler, no new dependency.** Considered pulling in a plotting/charting library (matplotlib, Chart.js, Plotly) but rejected: matplotlib's SVG export doesn't give hover interactivity at all (wrong tool for what's actually being asked for), and a full JS charting library is real weight for what's fundamentally two line series with point-hover tooltips. Instead: Python computes the two polylines (points normalized into a fixed SVG viewBox) plus small hover-target circles at each sampled point, embedded as inline `<svg>` markup — consistent with `fetch_hike_data.py`'s existing "standard library only, no pip install required" charter. A small inline `<script>` block (plain JS, no framework) handles `mouseenter`/`mousemove` on the hover targets to position and fill a tooltip — the one deliberate, minimal exception to hike-izer's zero-JS convention, scoped narrowly to this one interactive feature.

**Open implementation-level defaults (proposed, not yet built — flag if these should go differently):**
- **Point series downsampling:** raw GPS points (~30s cadence) would make for a cluttered/noisy hover target density. Default: resample the chart series to a fixed number of points along cumulative distance (e.g. every ~0.05 mi, capped around 50-100 points for a typical hike) rather than plotting every raw GPS point — smooths the hover experience without a smoothing pass on the underlying data itself.
- **Template placement:** new stat values (Ascent, Descent, Moving Time, Stopped Time, Total Time, Pace, Moving Speed, Avg Speed) as a new stat-row section (auto-fill grid, same CSS pattern as Weather Forecast) placed near Data Summary, not crammed into the existing 4-card hero — hero stays Date/Duration/Distance/Elevation Gain as-is. New "Elevation & Speed" `<section>` for the chart itself, placed after the enriched stat block.
- **Where the new stats/computation live:** extend `compute_stats()` in `fetch_hike_data.py` with the new numeric fields (ascent_ft, descent_ft, moving_time_min, stopped_time_min, pace_min_per_mi, moving_speed_mph, avg_speed_mph — total_time_min already exists as session duration), plus a new function building the downsampled chart-ready point series (distance_mi, altitude_ft, speed_mph, timestamp per sample). The actual SVG/JS markup generation is a separate new function/step (candidate: a small new module, e.g. `build_hike_chart.py`, or a function inside `fetch_hike_data.py` — leaning toward a separate module so `fetch_hike_data.py` stays "data + math," not "data + math + markup") — final split decided at Build time.

**Data source:** existing GPS Track data (`fetch_hike_data.py`'s `gps_rows` — timestamp/lat/lon/altitude per point, already flowing through the same pipeline CARD-0082 uses) — ascent/descent, speed, and moving-vs-stopped time are all derivable from consecutive-point deltas; no new data collection needed.

**Mockup approved 2026-08-01** — design mockup (chart + richer stat block, placed below where CARD-0082's map will eventually go, matching hike-izer's real CSS tokens/typography) reviewed and approved ("looks great for iteration 1") before Build started.

**Built 2026-08-01, not yet verified against a real hike — smoke-tested only:**
- `components/hike-izer/fetch_hike_data.py`: new `_hike_point_series()` (shared per-point distance/altitude/speed walk, reused by both the stats and the chart so neither can silently drift from the other), `_ascent_descent_ft()` (5ft noise-threshold filter per the Planning decision), `_moving_stopped_time_min()` (reuses the existing `WALKING_SPEED_MIN_MPS` cutoff), `compute_hike_detail_stats()`, and `build_chart_series()` (distance-resampled, capped at 80 points). Wired into `main()` right alongside the existing post-hoc `stats['distance_mi']` assignment, same `hike_confirmed`-gated "None means not available" convention as every other figure on the page. New `chart_series` key added to the JSON output.
- New `components/hike-izer/build_hike_chart.py`: `build_chart_html(chart_series)` renders the complete chart section (legend, hover tooltip slot, SVG with baked-in geometry: gridlines, axis labels, both polylines, per-point hover hit-targets carrying pre-baked `data-time`/`data-value`/`data-metric` attributes) plus the one small hover `<script>` — stdlib-only, no new dependency, matching the Planning-stage design. Geometry is computed once in Python at generation time; the JS only wires up events and reads attributes, never recomputes anything. MST time formatting done by hand (avoids `strftime`'s non-portable `%-I`/`%#I` flags between Linux/Windows).
- `components/hike-izer/html-template.html`: added the chart color tokens (light + dark), `.stat-row--rich` grid, and all `.chart-*`/`.hit-target`/`.hover-dot`/`.axis-*` styles from the approved mockup. New "Elevation & Speed" `<section>` (with a `{{ELEVATION_SPEED_CHART}}` placeholder for `build_chart_html()`'s output) and "Pace & Elevation Detail" `.stat-row--rich` section, both placed right after the hero stat row — with an explicit comment marking that CARD-0082's still-unbuilt map section goes immediately above the chart, not a placeholder map itself. Verified structurally sound (`html.parser` parses clean, section tags balanced).
- `.claude/skills/hike-izer/SKILL.md`: documented the new `stats` fields and `{{ELEVATION_SPEED_CHART}}` mapping in step 5, corrected two stale forward-references to CARD-0110 as a "future card" (one in the narrative-writing guidance, one in the "explicitly out of scope" list) now that it's built — the out-of-scope list now correctly separates CARD-0082's still-deferred map from CARD-0110's now-shipped chart, which are easy to conflate since both were originally grouped under "Levels 3-5."
- **Verification so far:** synthetic GPS data through the full pipeline (`fetch_hike_data.py`'s new functions → `build_hike_chart.py`) end-to-end with no errors, sane non-NaN SVG path output, correct zero-division guards (`moving_speed_mph` stays `None` rather than crashing when moving time is 0). Not yet run against a real hike's actual data end-to-end, and the real generated page's hover behavior hasn't been clicked through in a browser yet (only the standalone mockup was, which used hand-authored data rather than this code path) — both still needed before calling this Done.

**Verified against real data, 2026-08-01 (July 30, 2026 hike) — a real bug found and fixed, not just a clean pass:**
- Fetched the real July 30 GPS Track via `fetch_hike_data.py` against the live Apps Script endpoint (97 points) — two separate `is_hike` sessions that day (35.6 min / 1.33 mi, then a second 12.8 min / 0.52 mi session ~8 hours later), not the single-session shape every synthetic test so far had used.
- **Real bug caught by this real data, not the synthetic tests:** `stopped_time_min` came out to **504.1** (over 8 hours) on the first real run. Root cause: `_hike_point_series()` walked the flat, session-scoped-but-concatenated `altitude_gps_rows` as one continuous sorted sequence — the multi-hour gap *between* the two sessions became one giant near-zero-speed "interval," misread as stopped time instead of being recognized as time between two separate walks. `stats['distance_mi']` was never affected (it already summed per-session distances via `coverage['gps_track']['sessions']`), but `_hike_point_series()`'s own point-to-point walk had no session-boundary awareness at all.
- **Fix:** new `_session_point_series()` re-splits the scoped rows back into their own per-session buckets (using each session's own `[start, end]` window) before building each session's point series independently. `compute_hike_detail_stats()` now sums ascent/descent/moving-time/stopped-time *across* sessions rather than walking one flattened list. `build_chart_series()` does the same per-session downsampling, carries cumulative distance forward across the gap (so the chart's distance axis still reflects the full day's 1.85 mi), and tags the first point of every session after the first with `session_break: true`. `build_hike_chart.py` splits its elevation/speed polylines (and the elevation fill) into separate SVG subpaths at every `session_break` — confirmed via the real data's actual rendered path: exactly 2 subpaths (`M` count) on both lines, matching the 2 real sessions, no phantom line connecting the two walks.
- **Corrected stats after the fix, real July 30 numbers:** distance 1.85 mi, ascent 441 ft / descent 409 ft, moving 45.8 min / stopped 2.6 min (45.8 + 2.6 = 48.4, matching the summed session durations exactly — internally consistent now), pace 26:16 /mi, moving speed 2.4 mph, avg speed 2.3 mph.
- **Open question raised, then resolved same day:** ascent/descent (441/409 ft) looked high relative to the day's actual elevation range (594-662 ft, only 68 ft of min-to-max spread). Root cause confirmed, not just suspected: Joseph noticed several 6+ mph "walking" spikes on the speed chart and asked why. Traced every spike (>=5.5 mph) in session 1 back to its raw GPS rows — all three lined up exactly with a degraded fix (`accuracy_m` jumping to 21-35m at one endpoint, versus single-digit meters everywhere else in the track). A real session's own `accuracy_m` distribution turned out to have a clean natural gap (62 of 71 points <=14.0m, then a cluster of 9 points at 21.3m+) — confirms the same noise was also leaking into the 5ft `ASCENT_DESCENT_NOISE_FT` altitude filter, since position and altitude degrade together on a bad fix.
- **Fixed:** new `GPS_ACCURACY_MAX_M = 20.0` (grounded in that real gap, not guessed) in `fetch_hike_data.py`. `_hike_point_series()` now drops any point whose `accuracy_m` exceeds it *before* computing any interval, so distance/speed spans from the last known-good fix to the next one instead of using a bad fix as an endpoint — missing `accuracy_m` is kept, not dropped (absence isn't evidence of a bad fix). Fix propagates automatically to every consumer (ascent/descent, moving/stopped time, the chart series, and CARD-0082's route line/map hover) since they all read from this one shared function. **Verified on real session-1 data:** 8 of 71 points filtered out, max speed dropped from 6.7 mph to 4.1 mph (zero intervals >=5.5 mph, down from 3), ascent/descent dropped from 236/227 ft to 165/156 ft.
- **Still noisy after the accuracy filter — Joseph caught it ("even 4.1mph is high, seems noisy"), fixed same day.** A single accuracy cutoff only catches the worst-degraded fixes; the underlying problem is that point-to-point speed/altitude is inherently noise-sensitive at ~30s intervals, since even a "decent" 5-14m of GPS uncertainty is a large fraction of the ~30m a hiker actually covers in that time. Root-caused the remaining 4.1mph point to a 13.99m-accuracy fix (under the 20m cutoff, but still elevated) before building a fix, not just guessing. **`SPEED_WINDOW_MIN_SEC = 60.0`:** `_hike_point_series()` now computes each point's speed over a wider backward-looking baseline (widen until the span covers >=60s) instead of the single preceding interval — real distance scales with time, GPS noise doesn't, so a wider baseline dilutes the same jitter against more real distance. Falls back to the raw pairwise speed only for the first ~60s of a session (not enough window history yet) specifically so `moving_time_min + stopped_time_min` still sums to the exact session duration — confirmed live (35.6 + 0 → 30.9 + 4.7 = 35.6, exact). New field name `interval_dt_sec` (was `dt_sec`) makes the raw-time-vs-windowed-speed split explicit in the data itself. **Verified:** max speed dropped from 4.1 mph to 3.5 mph, zero intervals >=4.0 mph.
- **Ascent/descent validated (and then fixed) against real Gaia GPS ground truth for the identical hike, not just internal plausibility.** Joseph provided Gaia's own numbers for session 1: Total Time 32m29s, Stopped 2m31s, Moving Speed 2.5mph, Avg Speed 2.3mph, **Ascent 52ft, Descent 31ft**. Confirmed the ascent/descent gap was real and large — 165/156 ft reported here was over 3x Gaia's numbers, not just cosmetically off. Same fix shape as speed: widen the baseline, this time via a **centered** moving average (not one-sided — the whole session's points are already fetched, so smoothing with "future" points costs nothing and works better than a backward-only window of the same width) replacing each point's `alt_ft` in place, so every consumer (ascent/descent, the chart's displayed elevation line, map hover) sees one consistent smoothed value. Started from `SPEED_WINDOW_MIN_SEC`'s own 60s on the assumption vertical and horizontal GPS noise share a characteristic timescale — **wrong, per a real calibration sweep** against Gaia's numbers: 60s barely moved it (151/142), while 120s/180s/300s/600s gave 65/52, 52/38, 45/28, 24/11 — **180s** is the closest match (ascent hits Gaia's 52 exactly), both narrower and wider windows over/undershoot. `ALTITUDE_SMOOTH_WINDOW_SEC = 180.0`, calibrated against this one real hike, not yet cross-validated against a second — flagged as a starting point, not a settled constant. **Final verified numbers: ascent 52 ft (exact match to Gaia), descent 38 ft (vs Gaia's 31), moving speed 2.6 mph (vs 2.5), avg speed 2.2 mph (vs 2.3)** — all within real GPS-tracker-to-GPS-tracker agreement range.

**Known gap, not fixed this pass — Duration/Stopped Time run longer here than Gaia's, extent measured precisely:** Total Time 35m36s here vs Gaia's 32m29s (**3m07s longer**); Stopped Time 4m42s here vs Gaia's 2m31s (**2m11s longer**) — almost the entire duration gap (2m11s of 3m07s) shows up as extra *stopped* time, not extra *moving* time: Moving Time here is 30.9 min vs Gaia's implied ~30.0 min (32m29s total − 2m31s stopped), i.e. within about a minute. That pattern points at session-boundary trimming, not a noise/accuracy problem: this card's session start/end come straight from GPSLogger's own raw first/last trackpoint (grouped by a 10-minute gap), which likely includes a couple of minutes of standing-still time before/after the hiker actually starts walking, while Gaia appears to trim that idle time from its own reported start/end more aggressively. Not investigated further or fixed this pass — a real follow-up if it matters, but a session-boundary-definition question, not a data-quality one like the two fixes above.

**CARD-0110 marked Done, 2026-08-01** — chart, richer stats, hover interaction, and the underlying GPS-noise fixes are all built and verified against real data (including real Gaia GPS ground truth, not just internal plausibility). The Duration/Stopped-Time gap above is a known, separate, minor limitation, not a blocker on this card's own scope.
- **Side-by-side layout added, same day:** Joseph tried the shipped chart+map together and found the hover-sync, while working correctly, was hard to observe with the two sections stacked and other content between them. New `.hike-visuals` CSS grid in `html-template.html` wraps the Route Map and Elevation & Speed sections together — side-by-side at >=52rem viewport width (map's `.hike-map` height reduced to 15rem in that layout so the two panels read as a matched pair), stacked below that. Both sections share the same empty/non-empty condition already (`chart_series`), so the whole `.hike-visuals` wrapper is omitted together on an unconfirmed day, not independently — `SKILL.md` updated to say so explicitly.
- **Hover interaction verified functionally in a real browser** (Chrome via `claude-in-chrome`, real July 30 data, not the hand-authored mockup data): dispatched real `mouseenter`/`mouseleave` DOM events against the generated page's actual hit-targets, both before and after the accuracy-filter fix. Confirmed: hovering an elevation point shows that point's time + elevation and lights up only the elevation dot (speed dot stays hidden); hovering a speed point shows time + speed and lights up only the speed dot; `mouseleave` correctly resets to the default "Hover the chart —" state; side-by-side layout confirmed at wide viewport (both panels same vertical offset). Screenshot capture itself was flaky in this session (extension-side timeout, unrelated to the page), so this was verified via direct DOM/event inspection rather than a visual screenshot — real generated page kept up to date as an artifact for a visual look: https://claude.ai/code/artifact/8882621b-bccb-4787-9096-5220b6a33f27

**Related:** CARD-0082 (route map + elevation profile, the visual/map-focused sibling of this stats-focused card, same underlying GPS Track data source and the same `GPS_ACCURACY_MAX_M` fix benefits its route line/map), CARD-0104 (the Gaia-embed precedent and its "not automatable" caveat), CARD-0109 (removed the old single Elevation Range row from Data Summary, freeing that space for this card's richer stats), `components/hike-izer/fetch_hike_data.py`, `components/hike-izer/build_hike_chart.py`, `components/hike-izer/build_hike_map.py`, `components/hike-izer/html-template.html`, `.claude/skills/hike-izer/SKILL.md`.

---

**Archived from `tos/kanban-board.md` on 2026-08-22 (CARD-0193)** — 17297B, over the 10000B size threshold.

### CARD-0085 · [idea] [hike-izer] Direction of travel (GPS bearing) + sun-position Route Map gadget
**Status:** Done

**Notes:** Raised 2026-07-23 as "Hiker's own compass/heading," split out of CARD-0074 (Hike-izer v2, superseded). Originally scoped around the hiker's actual *facing* direction (sun position relative to the hiker — sun in your eyes, sun at your back — not just the sun's absolute compass position, which v1 already computes from pure astronomy). That would have needed new hardware (a magnetometer/compass sensor) or another data source, since nothing captures body-facing today.

**Rescoped 2026-08-01 (Joseph's call):** facing direction isn't worth pursuing — unclear what any deviation from direction-of-travel would actually be used for. **Direction of travel is good enough.** This drops the hardware requirement entirely: Android's Location API already computes a `bearing` field on-device from GPS fixes, and GPSLogger (the app already generating this pipeline's trackpoints) exposes it as the `%DIRECTION` placeholder in its HTTP POST template. Today's pipeline never requests it — `environmental-data.gs`'s `action=gps` handler (`doGet`) only parses `lat`/`lon`/`acc`/`alt` from GPSLogger's POST and appends those four to the GPS Track sheet, and the Tasker/GPSLogger config was only ever wired for those same four fields. Adding `%DIRECTION` on both sides (Tasker config + `doGet` + GPS Track sheet column + `fetch_hike_data.py`'s row parsing) is now the whole scope — no hiking-monitor hardware tie-in, no magnetometer.

**Blocking dependency dropped 2026-08-01:** the original hiking-monitor-hardware blocker doesn't apply — like CARD-0084/CARD-0086's own corrections, GPS trackpoints come from GPSLogger on the phone, entirely independent of the ESP32 hiking-monitor device's status.

**Split into two parts, 2026-08-05 (design discussion while validating CARD-0144's sun-position math, which this card's own foundation depends on):**

**Part A — Sun-position Route Map gadget, buildable now (moved to Build).** Doesn't need travel-direction data at all -- only needs the sun's own position (azimuth/elevation), which `fetch_hike_data.py` already computes deterministically via `solar_position()` (no API cost). Design, worked out across the discussion:
- A small corner gadget on the Route Map (a proper Leaflet custom control, `topright` -- not a raw absolutely-positioned div, so it can't collide with Leaflet's own zoom control and needs no manual z-index guessing) showing a hollow (stroke-only, matching the existing `_MARKER_ICONS` line-art convention) arrow indicating the sun's position.
- **Rotation, not an arbitrary length formula:** `transform: rotateX(elevation_deg) rotateZ(azimuth_deg)` on a `perspective`-wrapped, `transform-style: preserve-3d` element, hinged at the arrow's base (`transform-origin` near the bottom). `rotateZ(azimuth)` points the arrow in the flat map-plane toward the sun's compass bearing (applied first, innermost); `rotateX(elevation)` then tilts that whole arrow up out of the plane toward the viewer by the sun's actual elevation angle (applied second, outermost) -- a real physical rotation, not a hand-picked `cos(elevation)` length scale: at elevation 0° the arrow lies flat (full apparent length, sun on the horizon), at elevation 90° it's tilted a full quarter-turn out of the plane (foreshortened to near nothing, sun overhead) -- the browser's own 3D projection produces the "long when low, short when high" effect for free. Optional reinforcing touches: a `filter: drop-shadow()` that shrinks with the tilt (echoing a real shadow), a subtle warm-to-cool color shift by elevation.
- **Hover-synced, not static:** reuses the *existing* Route Map hover-sync mechanism verbatim (`showIndex(i)`/`hide()` in `build_hike_map.py`, already wired to `hikeizer-map-hover`/`hikeizer-chart-hover` CustomEvents for CARD-0110's chart pairing) -- hovering a route point updates the gadget to that point's own sun position; the rest/default state (mouse not hovering) shows the hike's **start** position, since `hide()` already runs once at init and is the natural place to set that default.
- **Data plumbing, minimal:** add `sun_azimuth_deg`/`sun_elevation_deg` to each point `build_chart_series()` (`fetch_hike_data.py`) already emits -- that's the one shared series both the Route Map and the Elevation & Speed chart read (per CARD-0082's own docstring), so this keeps sun data correctly index-matched to whatever points are actually hoverable, rather than relying on the separately-sampled (and differently-decimated) `sun_position_samples` array used for the Sun Data Summary table.

**Built, deployed, and structurally verified live, 2026-08-05 18:57 MST.** `build_chart_series()` now computes `sun_azimuth_deg`/`sun_elevation_deg` per point; `build_hike_map.py` adds the `SunGadgetControl` (Leaflet `topright` control), the hollow arrow SVG, and wires `updateSunGadget()` into both `showIndex()` (hover) and `hide()` (defaults to the hike's own start point); `templating.py`'s `_HTML_STYLE` carries the `.sun-gadget`/`.sun-gadget-arrow` CSS. Deployed to the M8 (`scp` + `docker compose up -d --build orchestrator`), regenerated `2026-08-05_hike-summary.html` in place (same pattern as CARD-0140/CARD-0144), and confirmed all the expected pieces landed on the live page: `SunGadgetControl`, the `.sun-gadget`/`.sun-gadget-arrow` CSS, and real `sunAzimuth`/`sunElevation` values (89.4°/24.2° for this hike's start point) in the JS points array. **Not yet visually confirmed in a live browser** -- this verification only checked that the correct markup/CSS/JS/data exist, not how the 3D tilt/rotation actually reads visually (gadget size, transform-origin precision, arrow proportions); that's a subjective call worth Joseph's own look before considering this fully done.

**Real bug found and fixed, 2026-08-05 19:19 MST:** Joseph looked at the live `2026-08-05` page -- arrow correctly pointed east, but showed zero 3D tilt ("just a line arrow"). Root cause, confirmed by hand via the actual rotation matrices (not guessed): the original transform order, `rotateX(tilt) rotateZ(azimuth)`, applies the compass spin *first* (innermost) and the tilt *second* (outermost, in the world/screen frame) -- fine for a N/S-pointing arrow, but for azimuth near 90/270 (E/W) the rotateZ has already spun the arrow's shaft to align almost exactly with the world X-axis, which is the very axis rotateX pivots around -- rotating a stick around its own long axis can't make it look shorter. This hike's real azimuth (89.4°, essentially due east) landed exactly on that failure case, which is why the symptom was specifically "no tilt," not just "subtle tilt." **Fix:** swapped the order to `rotateZ(azimuth) rotateX(tilt)` -- tilt now applies *innermost*, foreshortening the arrow while it's still in its own rest orientation (shaft aligned with the axis rotateX actually affects), and the compass spin (a pure in-plane rotation) then preserves whatever foreshortening the tilt already produced, regardless of azimuth. Also added a `TILT_EXAGGERATION = 2.2` multiplier (capped at 85°) -- a literal `elevation_deg` tilt is real physics but visually subtle for most of a hike's actual elevation range (24° only foreshortens to ~91% of full length, barely perceptible at icon size); exaggerated so the gadget reads clearly at a glance, which is the point of it, over strict physical accuracy.

Verified via the computed transform matrix on a fresh page load (not live-browser screenshot -- the screenshot tool hit a persistent, unrelated timeout this session): `rotateZ(89.4deg) rotateX(53.24deg)` (53.24 = 24.2 x 2.2) produces on-screen projected length `cos(53.24°) ≈ 0.598` -- a genuine ~40% foreshortening, confirmed mathematically from the actual `matrix3d()` values rather than assumed. Deployed to the M8 and `2026-08-05_hike-summary.html` regenerated in place again.

**Second real bug found and fixed, same session, 2026-08-05 19:24 MST:** Joseph looked again -- tilt now worked, but the arrow still pointed east, and he correctly called out a genuine conceptual error: the gadget is meant to show *"the direction the sun is shining"* -- the direction its rays travel -- not the direction to look to find the sun. Those are opposite: a sun sitting in the east shines its light toward the **west**. The arrow was pointing at the sun's own azimuth (toward it) instead of the ray direction (away from it, `azimuth + 180`). Fixed in `updateSunGadget()`: `rayDirection = (p.sunAzimuth + 180) % 360`, rotateZ now uses that instead of the raw azimuth. Verified via the computed matrix on a fresh reload: `rotateZ(269.4deg)` (89.4 + 180), pointing due west as expected. Deployed and regenerated again. **Still needs Joseph's own visual confirmation of the corrected direction.**

**Arrow glyph replaced, 2026-08-08 07:56 MST (Joseph's call):** didn't like the thin hollow line-art arrow -- wanted something chunkier/more visible, referencing a bold solid-fill arrow style from a screenshot. `_SUN_GADGET_ARROW_SVG` (`build_hike_map.py`) replaced with a hand-drawn solid `fill="currentColor"` arrow (wide triangular head, thick rectangular shaft) -- not traced from the referenced image, which was licensed stock art (Vecteezy Pro), but an equivalent generic shape drawn from scratch. Deliberately kept vertically balanced around the viewBox's own center (spans y=2 to y=22) rather than anchored to its base: confirmed from `templating.py`'s own CSS comment that `.sun-gadget-arrow` pivots on `transform-origin: center`, not the arrow's base (a past fix -- a bottom anchor made the arrow sweep like a clock hand under compass rotation) -- an off-center shape would have looked wrong under that same rotation. Only this one SVG changed; `_TRAVEL_ARROW_SVG` (Part B's route-line arrows, still hollow line-art) deliberately left alone -- Joseph's ask was scoped to the sun gadget specifically.

Deployed to the M8 (`scp` + `docker compose up -d --build orchestrator`) and regenerated `2026-08-05_hike-summary.html` in place -- but surgically this time, not a full `render_html()` re-run: re-used the page's own already-computed `chart_series` (straight from the persisted `hike_data.json`, free/deterministic, no re-fetch) and its own already-rendered `eventMarkers` (extracted from the live HTML's embedded JS array and converted back to the snake_case shape `build_map_html()` expects), so only `build_hike_map.build_map_html()`'s output changed -- photos, narrative, place-context, and BirdNET data were never touched or recomputed, avoiding any Immich/Claude API cost for what's a purely cosmetic change. Verified the replacement targeted exactly one, correctly-bounded `<section>` (checked byte offsets directly rather than assuming a regex matched right) before writing. Confirmed live: new arrow path present exactly once on both the container's file and the public `https://hikes.jctnet.com/2026-08-05_hike-summary.html`, old arrow path completely absent, section count unchanged (9), Route Map and Elevation headers both intact. **Not yet visually confirmed in a live browser** -- same caveat as the original build.

**Part B — travel-direction data capture: confirmed working end-to-end, 2026-08-05 18:54 MST.** Correction to the original notes above: this pipeline was never actually a Tasker integration -- `%DIRECTION` is configured directly in the **GPSLogger app itself** (Logging Details → Log to custom URL), no Tasker profile involved at all (that's why Joseph couldn't find it in Tasker when he went looking). Joseph added `&direction=%DIRECTION` to that URL and the `direction` header to GPS Track column F; Claude updated `environmental-data.gs`'s `doGet()` (`action=gps`) to parse and append it (`e.parameter.direction !== undefined ? parseFloat(...) : ''` -- same "missing isn't evidence of a bad fix" convention `fetch_hike_data.py` already applies to `accuracy_m`, so pre-existing rows without this column don't break) and bumped `SCRIPT_VERSION` to `2026-08-05.1-gps-direction`. Verified three ways: (1) `action=version` confirmed the redeploy actually took effect: (2) a synthetic `action=gps` test call with `direction=274.5` round-tripped correctly through `action=export`'s generic column-mapping, landing as a properly labeled `direction` field (an older pre-existing row correctly showed `''` instead, confirming backward compatibility); (3) **a real GPSLogger test walk** -- Joseph confirmed real rows now carry a real (non-blank) direction value, the one thing only a live device could prove. `gps-pipeline.md` updated to match (placeholder table, column-header table, URL example, corrected Tasker->GPSLogger-native framing).

**Part B display, rescoped 2026-08-05 (Joseph's call):** a "Direction of Travel" table row was tried first (mirroring the existing "Sun Direction" row) and explicitly rejected -- "It's not a row but a tiny arrow on the map." Via AskUserQuestion, Joseph picked **small arrows placed along the route itself** over a single corner gadget (confirmed "arrows, more than 1" -- several arrows spaced along the trail, not one indicator).

**Built and deployed, 2026-08-05 (same session).** `fetch_hike_data.py`: raw `bearing_deg` pass-through added to `_hike_point_series()` (`to_float(r.get('direction'))`, no smoothing -- unlike altitude this is already a real instantaneous per-point value the phone reports directly) and `travel_bearing_deg` added to `build_chart_series()`'s output alongside the existing sun fields. `build_hike_map.py`: new `_TRAVEL_ARROW_SVG` (same hollow-arrow visual vocabulary as the sun gadget's arrow, but plain 2D -- no perspective/tilt, since travel direction has no elevation-angle analog), `_select_travel_arrow_points()` (evenly-spaced subset, `TRAVEL_ARROW_COUNT = 10`, skips points with no real bearing rather than guessing one from neighboring lat/lon), and a new non-interactive `L.marker`/`L.divIcon` render loop with each icon's `rotate()` set inline to that point's own bearing. `templating.py`: `.travel-arrow-marker`/`.travel-arrow-icon` CSS (transparent background, no circular badge -- a route-line annotation, not a discrete clickable event like the photo/bird markers).

**Verified two ways, not yet confirmed visually on a real hike with real bearing data:** (1) unit-tested `_select_travel_arrow_points()` and the JSON payload directly with synthetic bearing data -- confirms 10 evenly-spaced points selected and correct lat/lon/bearing values reach the `travelArrows` JS array; (2) deployed to the M8 (`scp` + `docker compose up -d --build orchestrator`) and regenerated `2026-08-05_hike-summary.html` in place, which renders `travelArrows = []` -- **zero arrows, correctly**, not a bug: this specific hike's own `gps_track` rows all have `direction: ''`, because it ran 2026-08-05 12:39-13:38 UTC, before Joseph's later `%DIRECTION`-enabled GPSLogger test that same day. **Still needed before calling this done:** a real hike recorded *after* `%DIRECTION` was actually flowing, to confirm the arrows render and rotate correctly against real device data, the same rigor Part A's three bugs got caught by.

**Blocked, 2026-08-08 07:56 MST (Joseph's confirmation):** the entire remaining scope on this card is Part B's real-data verification above -- code is written, deployed, and unit-tested, but genuinely unproven, and nothing left to build in the meantime. Waiting on an actual hike walked with `%DIRECTION` GPS logging live (already confirmed working end-to-end 2026-08-05) to confirm the arrows render/rotate correctly against real bearing values. Part A (sun gadget) is otherwise in a reasonable state ("looks okay for now," Joseph, 2026-08-08) but still carries its own separate, lighter-weight open item -- live-browser visual confirmation, never blocking, just not yet done.

**Unblocked and closed out, 2026-08-10 (Joseph's hike that morning provided the real data this was waiting on).** `2026-08-10_hike-summary.html` published with real, non-empty `travelArrows` data -- 10 genuine bearing values (0.0-265.5°, not the placeholder-empty `[]` the 2026-08-05 hike produced), confirmed rendering correctly on the live map and visually confirmed by Joseph. Both parts of this card are now proven against real data end-to-end. Moved to Done -- further visual/style iteration on the travel arrows (smaller, lighter weight, offset parallel to the track rather than on it) continues under CARD-0147 rather than reopening this one, since that's a refinement of the already-working feature, not new pipeline scope.

**Related:** CARD-0073 (Hike-izer v1, Done), CARD-0074 (superseded), CARD-0082 (Route Map, the hover-sync/shared-`chart_series` machinery Part A reuses), CARD-0110 (Elevation & Speed chart, the other half of that hover-sync pairing), CARD-0133 (event-marker icon convention Part A's hollow arrow matches), CARD-0144 (the sun-azimuth bug fix this whole design discussion surfaced, and whose correctness Part A depends on), `components/hike-izer/fetch_hike_data.py`, `components/hike-izer/build_hike_map.py`, `core/data-pipeline/environmental-data.gs`, `components/hiking-monitor/gps-pipeline.md` (corrected/updated for the `direction` column and GPSLogger-native, not Tasker, config path).

---

**Archived from `tos/kanban-board.md` on 2026-08-22 (CARD-0193)** — 15970B, over the 10000B size threshold.

### CARD-0082 · [idea] [hike-izer] Visual track + elevation graphic, Gaia-GPS-style
**Status:** Done

**Sequencing update 2026-07-28:** Joseph is trying **CARD-0104** (embedding Gaia GPS's own track view directly, since he already uses Gaia on every hike) first — much less engineering than this card's from-scratch render. This card stays available as the heavier fallback: fully automatable and self-contained (no dependency on Gaia's servers/account staying available), if CARD-0104's embed approach doesn't pan out or a fully-automatic/self-hosted version is wanted later. Not blocked, not deferred indefinitely — just not next.

**Notes:** Raised 2026-07-23. Add a visual graphic depicting the hike route and elevation profile, in the style of Gaia GPS's track view — route line plotted over a real topo/satellite basemap, paired with a distance-vs-elevation chart. This card owns the embedded-visuals and interactive-hover-sync work directly (CARD-0088, once scoped as an intermediary "embedded visuals" level, was narrowed 2026-07-24 to just HTML hosting after realizing it was pure duplicate scope of this card) — this is its own standalone artifact so it can potentially be embedded as a static image in the *current* Markdown output too, not gated on any other card's work landing first. (Note: the Markdown-output reference predates CARD-0091, 2026-07-28 — HTML is now the sole output format; this card's static-image level would embed there instead.)

**Target experience (end goal):** route line + elevation profile shown together, with interactive sync between them — hovering the elevation chart highlights the matching point on the map, matching Gaia GPS's actual behavior.

**Iteration path (agreed 2026-07-23):**
1. **Static image, real basemap** — starting point. A single PNG (or similar) with the GPS route plotted over an actual topo/satellite basemap, plus a separate (not yet linked) elevation profile chart. Embeddable in Markdown output today via standard image syntax.
2. **Interactive, hover-synced** — the real Gaia GPS experience (map ↔ elevation profile linked via hover/scroll). This requires JS/HTML, so naturally becomes an embed within the HTML output CARD-0081 established (Levels 1-2, Done), once this level is built, rather than a standalone file.
3. **Event markers** (added 2026-07-23) — drop markers on the map for photos, hike observations, and other timestamped/geotagged events (bird sightings once CARD-0080 lands are an explicitly confirmed case, not just a hypothetical). Design the marker system generically/extensibly rather than hardcoding a fixed list of event types — whatever ends up timestamped and geotagged in the pipeline should be markerable, not just the types known today. Markers for photos specifically are blocked on CARD-0084 (photo integration, not yet built) — the marker mechanism itself doesn't have to wait, but that particular marker type has nothing to plot until photo data exists.

**Basemap decision (agreed 2026-07-23):** a real topo/satellite basemap is required even for the first (static) iteration — not deferred. Candidate tile providers to evaluate when this is picked up (none confirmed yet):
- **OpenStreetMap tiles** — free, no API key, but subject to OSM's tile usage policy (fine for personal/low-volume use, not for heavy automation); would need a stitching library (e.g. Python `staticmap`) to render tiles + route overlay into one image.
- **Thunderforest "Outdoors" style** — topo/contour styling closest to Gaia GPS's actual look; free tier for personal/hobby use, needs an API key.
- **Mapbox Static Images API** — flexible styling, needs an API key, has a free tier (verify current terms/limits at implementation time, not assumed here).

**Data source:** existing GPS track pipeline (GPSLogger → GPS Track sheet, already wired into `components/hike-izer/fetch_hike_data.py`) — no new data collection needed, this is purely a new rendering of data hike-izer already has.

**Re-scoped for Planning, 2026-08-01 — both original staging assumptions no longer hold.** The "start with a static image" plan was written for two reasons that are both now gone: (1) static-image embeddability in Markdown output, which stopped existing once CARD-0091 made HTML the sole output format; (2) no interactivity precedent existed yet in hike-izer's template. CARD-0110 (Done, 2026-08-01) has since shipped a real interactive Elevation & Speed chart with hover, establishing that a small, scoped amount of client-side JS is acceptable when a feature genuinely needs it. Since the Target Experience above explicitly wants hover-sync with that chart, building a throwaway static-image renderer first no longer makes sense — **decided: go straight to an interactive map**, skipping the original Level 1.

**Build approach, decided 2026-08-01 — Leaflet, not server-side image composition.** Considered continuing the original static-PNG idea (Python-side tile stitching, e.g. the `staticmap` library floated below) versus a small client-side mapping library. Leaflet (MIT-licensed, ~40KB gzipped, no API key of its own) wins clearly once interactivity + hover-sync are in scope from the start: it handles tile loading, pan/zoom, route-line rendering, and marker/hover events natively, versus hand-building tile-stitching, lat/lon-to-pixel projection, *and* a custom interaction layer on top of a static image. This is hike-izer's second deliberate exception to its zero-JS-by-default convention (after CARD-0110's chart) — a real map with real interactivity is a big enough feature that reaching for the standard, well-trodden tool clearly beats hand-rolling it, the same reasoning CARD-0110 used to reject a full JS charting library for a simpler feature but in the opposite direction here given the feature is genuinely bigger.

**Basemap provider, decided 2026-08-01 — Thunderforest "Outdoors."** Closest visual match to Gaia GPS's own topo/contour styling (the whole point of "Gaia-GPS-style"), which OpenStreetMap's standard road-map tiles don't really deliver despite being zero-setup. Needs a free-tier API key — **action item for Joseph**: sign up at thunderforest.com for a free-tier account/API key (an account-creation step Claude won't do on his behalf); once obtained, it goes in `credentials.local.md` alongside every other API key in this repo, and gets read by the new map-generation script the same way `immich-update-check.py` reads its own credentials file.

**Technical design, Planning 2026-08-01 — grounded in CARD-0110's actual shipped code, not assumed:**
- **Single shared point series, not a second one.** `fetch_hike_data.py`'s `build_chart_series()` (CARD-0110) already computes exactly the per-point series this needs — timestamp, cumulative distance, altitude, speed, `session_break` — just missing lat/lon (which `_hike_point_series()` already computes internally but doesn't currently pass through). Add `lat`/`lon` to `build_chart_series()`'s output dicts; both the existing chart and the new map then read from the exact same `chart_series` array, so a given index means the same real point in both places — the same "one shared walk, can't drift" principle CARD-0110 used internally now extended across both visual components.
- **New module, `components/hike-izer/build_hike_map.py`**, mirroring `build_hike_chart.py`'s shape: `build_map_html(chart_series)` returns the ready-to-embed `<div id="hikeMap">` + inline `<script>` (Leaflet init, tile layer with the Thunderforest key, route polyline, hover wiring). Returns `''` when `chart_series` is empty, same "no empty scaffolding when hike_confirmed is false" convention as Photos and the Elevation & Speed chart.
- **Session breaks handled the same way as the chart:** split into segments wherever `session_break` is true and draw one `L.polyline()` per segment — no phantom line connecting two separate hike sessions on the map either, matching CARD-0110's already-solved (and real-data-verified) fix for the exact same problem.
- **Route rendering:** `map.fitBounds()` over all segments' lat/lons so the whole day's hiking is framed automatically, no manual zoom/pan needed per hike (this is exactly the "opens at continent-level zoom" failure mode CARD-0104 hit with the Gaia embed — `fitBounds` avoids it structurally rather than hoping the provider gets it right).
- **Hover-sync, both directions, via a small custom-event bridge rather than a shared JS module:** `build_hike_chart.py`'s existing hover script gains two new lines — dispatch a `hikeizer-chart-hover` `CustomEvent` (detail: that point's lat/lon) on hover, and `hikeizer-chart-unhover` on mouseleave. `build_hike_map.py`'s script listens for both to move its own highlight marker. Symmetrically, the map's route-line hover dispatches `hikeizer-map-hover`/`-unhover`, which the chart's script listens for to move its own dot and update its tooltip slot. Keeps the two modules independently generated and testable (matching CARD-0110's own module boundary), coupled only through two plain DOM events — no shared state object, no one script depending on the other's internals or load order.
- **Vendoring, not a live CDN reference:** Leaflet's JS/CSS/marker-icon assets get downloaded once and committed under a shared vendor path (e.g. `components/hike-izer/vendor/leaflet/`), deployed once to the M8's web root (`~/hike-izer-web-app/srv/vendor/leaflet/`) and referenced by every hike page via relative path — same pattern Photos already uses for its sibling `_photos/` directory, and avoids depending on a public CDN staying up for a page meant to stay valid indefinitely.
- **Template placement:** the `{{ELEVATION_SPEED_CHART}}` section in `html-template.html` already has a comment (added by CARD-0110) marking exactly where this card's Route Map section goes — immediately above the chart. Add the real `<section><h2>Route Map</h2>{{ROUTE_MAP}}</section>` there now, replacing that placeholder comment.
- **Event markers (original Level 3) — still a later phase, not solved in this pass.** Design intent (generic/extensible marker types, not a hardcoded list) carries forward unchanged from the original plan; sequencing after the base interactive map + hover-sync is proven live on a real hike.

**Built and verified against real data, 2026-08-01 (July 30, 2026 hike, same day CARD-0110 used) — a real bug found and fixed, not just a clean pass:**
- `fetch_hike_data.py`'s `build_chart_series()` extended with `lat`/`lon` per point (already computed internally by `_hike_point_series`, just not previously exposed) — the map reads the exact same `chart_series` array CARD-0110's chart does, so the two can never disagree about what a given point is.
- New `components/hike-izer/build_hike_map.py`: `build_map_html(chart_series, thunderforest_api_key)` renders the Leaflet map (tile layer, route polylines split by `session_break` exactly like the chart's lines, `fitBounds`, and hover hit-targets) plus the init + hover-sync `<script>`. Deliberately duplicates its own tiny `_mst_time_str`/`_esc`/segment-splitting helpers rather than importing from `build_hike_chart.py`, keeping the two modules independently generated/testable per the Planning-stage design — coupled only at runtime through two CustomEvents.
- `build_hike_chart.py` extended with `data-index` on every hit-target circle and the two new CustomEvents (`hikeizer-chart-hover`/`-unhover` dispatched on hover, `hikeizer-map-hover`/`-unhover` listened for for the reverse direction) — a `showBoth()` path handles the map-originated case (no "which line" concept there, so both dots light up together instead of one).
- Leaflet 1.9.4 vendored (JS/CSS/marker icons, verified genuine — not a redirected error page — before committing) under `components/hike-izer/vendor/leaflet/`, **deployed to the M8's web root** (`~/hike-izer-web-app/srv/vendor/leaflet/`) and confirmed reachable at the real public URL (`https://hikes.jctnet.com/vendor/leaflet/leaflet.js` → `200`).
- `html-template.html`: vendored Leaflet `<link>`/`<script>` tags in `<head>`, the real "Route Map" `<section>` (replacing CARD-0110's placeholder comment, confirmed placed above the chart per this card's own scope decision), `.map-card`/`.hike-map`/`.route-line`/`.route-highlight` CSS reusing the existing `.chart-tooltip-slot` language and `--accent` token for the route color.
- **Real bug caught by real data, not the synthetic-only pass:** first browser load threw `SyntaxError: Unexpected identifier 'https'` — the Thunderforest attribution string (`<a href="...">`) contains literal double quotes, and embedding it into the JS via `attribution: "{THUNDERFOREST_ATTRIBUTION}"` let those inner quotes terminate the JS string early. Fixed by switching both the attribution string and the tile URL to `json.dumps()`-based embedding instead of manual quoting — the same class of mistake CARD-0132/CARD-0110 haven't hit only because their embedded strings never happened to contain a `"`.
- **Verified live in a real browser (Chrome via `claude-in-chrome`, real July 30 data, both hover directions via actual DOM event dispatch, not just eyeballed):** hovering a chart point correctly updates the map's tooltip slot; hovering a map point correctly updates the chart's tooltip *and* lights up both its elevation and speed dots together; both directions clear correctly on unhover. All 97 GPS points rendered as two route-line segments (confirmed 2 polyline subpaths, matching the day's 2 real sessions, no phantom connecting line). Confirmed `.route-line`'s CSS class genuinely overrides Leaflet's default blue (`computedStroke` came back as the page's actual `--accent` green, not Leaflet's `#3388ff` default) — CSS beats Leaflet's presentation-attribute styling as expected. All 6 requested map tiles loaded successfully from the real Thunderforest endpoint with the real API key.
- **Not verified this pass:** a real visual screenshot (the browser screenshot tool was flaky all session, same as during CARD-0110's verification — DOM/event-level testing substituted), and a real end-to-end run through the actual hike-izer generation skill onto a real published page (this was a scratch verification copy, cleaned up afterward, not a real `hike-izer/summaries/` file).
- Side-by-side layout with the Elevation & Speed chart (`.hike-visuals` grid, CARD-0110) added and verified same day — see that card.
- GPS-noise fixes built for CARD-0110 (accuracy filtering, windowed speed, altitude smoothing) benefit this card's route line and map hover too, since both read from the same `_hike_point_series()`/`chart_series` — no separate work needed here.

**CARD-0082 marked Done, 2026-08-01** — the interactive map (basemap, route line, hover-sync with the Elevation & Speed chart) is the scope this card actually committed to at Planning ("go straight to interactive," skipping the original static-image Level 1). **Event markers (original Level 3 — photos, hike observations, bird sightings) were explicitly scoped as a later phase from the start and were never part of this card's own Done criteria — split out to CARD-0133, not blocking this card.**

**Related:** CARD-0104 (the Gaia-embed alternative, Done — this card's heavier, fully self-hosted/automatable sibling), CARD-0081 (HTML rendering this embeds into), CARD-0110 (Done — the Elevation & Speed chart this hover-syncs with, the `chart_series` data source this reuses, and the precedent for accepting scoped interactivity/JS), CARD-0084 (Photos, Done — unblocks the photo-marker case of CARD-0133), CARD-0080 (Bird ID — confirmed marker case of CARD-0133), CARD-0133 (event markers, split out as this card's deferred Level 3), CARD-0073 (Hike-izer v1, Done), `components/hike-izer/fetch_hike_data.py`, `components/hike-izer/build_hike_chart.py`, `components/hike-izer/build_hike_map.py`, `components/hike-izer/vendor/leaflet/`.

---

**Archived from `tos/kanban-board.md` on 2026-08-22 (CARD-0193)** — 12923B, over the 10000B size threshold.

### CARD-0080 · [idea] [hike-izer] Integrate bird species identified via Merlin Sound ID / BirdNET Live — RESOLVED 2026-07-29 17:18 MST
**Status:** Done

**Moved to Build, 2026-07-29 17:07 MST** — design and module architecture finalized above; implementation starting now.

**Notes:** Raised 2026-07-23. Joseph has been using Cornell Lab's Merlin Bird ID app (Sound ID feature — real-time audio-based species identification, 2000+ species) while hiking and wants that data folded into Hike-izer's narrative summaries, correlated with GPS location and time on the route (e.g. "heard a Canyon Wren near the summit around 2pm") — same treatment the existing GPS track and Environmental Data sources already get, not just a flat species list.

**Data source options considered 2026-07-23 (eBird-based, ruled out):**
1. **eBird bulk export** — "My eBird" → Download My Data → emailed link to a zipped `MyEBirdData.csv` covering all personal observations. Manual/periodic, not automatable on a per-hike basis without extra steps.
2. **eBird per-checklist export** — open a specific checklist → Checklist tools → Download → CSV for just that outing. More precise for matching one hike, still a manual per-hike action.
3. **eBird API** — programmatic access via a personal API key (same credential pattern already used elsewhere in this project — Immich, Apps Script). Described as "designed for limited, recent and summary outputs," not a full data-dump API. **Fundamental grain problem, not just an access-method detail:** eBird's core unit is a checklist tied to one location (or a rough traveling track), not a GPS point per species — even a per-checklist export wouldn't give per-detection coordinates, so this path can't actually deliver "heard near the summit around 2pm"-style correlation regardless of which export/API option is used.

**Key dependency — resolved 2026-07-24:** the eBird path requires Merlin's Sound ID results to actually be **submitted to eBird** as a checklist, which Joseph hasn't done and wasn't planning to commit to for this. Combined with the grain problem above, eBird is not the path.

**Fallback data source investigated 2026-07-24: Merlin's Life List, via screenshot.** Merlin itself has no export/API at all (confirmed, browsing-only). Its **Life List** (cumulative all-time record, not per-session) shows **date + precise address + species per entry directly in the scrollable list view** — no need to tap into individual entries. Workflow would be: after a hike, screenshot that day's Life List entries; Claude reads the screenshot directly (native multimodal vision — no OCR tooling, no transcription). Merlin gives **no time of day, date only** — the design worked out to compensate for this: geocode each entry's address to lat/lon, match to the **nearest point on that day's confirmed GPS track**, and borrow *that trackpoint's own timestamp* as the inferred sighting time. This is a real, workable fallback, but adds a geocoding dependency (service not yet picked) and inherently approximate timing.

**Delivery mechanism considered and rejected:** Quick Share (Android↔Windows, AirDrop-like) requires both devices awake and physically nearby at transfer time — real friction for reviewing a hike's sightings whenever convenient rather than immediately upon getting home. Better direction: auto-backup screenshots to Immich (photo-server) so delivery is decoupled from the laptop's state entirely — but `fetch_hike_photos.py`'s existing matching logic only searches within the hike's own GPS-tracked time window, and a post-hike Life List screenshot wouldn't fall inside that window, so bird-screenshot lookup would need separate matching logic (e.g. same-day screenshots taken after the hike ended) if this path is used.

**Primary path now under evaluation, 2026-07-24: BirdNET Live.** Same Cornell Lab research lineage as Merlin's Sound ID (shared underlying identification tech). Confirmed via research: real-time on-device offline bird ID, a **Survey Mode** built for exactly this (GPS tracking alongside continuous audio detection during a transect), and **direct export to CSV/JSON/GPX/ZIP** — structured data, not a screenshot. **Not yet confirmed:** whether the export has lat/lon + timestamp on each individual detection row, or only session/survey-level GPS — this is the one open question that determines whether it fully replaces the Merlin screenshot-and-geocode fallback above. **Next step: Joseph tries Survey Mode on a real hike, exports a CSV, and reports the actual column structure** — that decides which of the two data-source designs above this card actually builds against. iNaturalist was also considered (clean per-observation API with real per-sighting GPS+timestamp) but its workflow is deliberate/manual per-sighting rather than continuous background listening like Merlin/BirdNET Live, so it's a weaker fit for preserving the current hands-free hiking UX.

**Integration approach (decided):** correlate each bird ID with GPS location + timestamp from the existing hike-izer pipeline (`components/hike-izer/fetch_hike_data.py`), matching the treatment other data sources already get — not a flat unlinked species list.

**Scope:** kept as its own standalone card, not folded into CARD-0074 (Hike-izer v2) — distinct data source with its own open questions worth resolving independently. Not blocked on the hiking-monitor device being back in active rotation (CARD-0074's blocker) — the bird-ID app runs on Joseph's phone, independent of the ESP32 sensor hardware.

**Confirmed via real export, 2026-07-29 16:59 MST:** Joseph ran BirdNET Live Survey Mode on both of today's hikes and provided the exports. Real format is **JSON** (not CSV — the `.gpx` sibling is metadata-only, no track points; `.selections.txt` is a redundant flat duplicate of the same detection fields). Per detection: precise UTC `timestamp`, `commonName`, `scientificName`, `confidence` — but **no per-detection GPS**, only one session-level lat/lon for the whole survey. Resolves the open question from the note above: BirdNET Live doesn't hand us per-sighting GPS directly, but its precise per-detection timestamp is enough to correlate against hike-izer's own already-fetched raw GPS track (`hike_data['gps_track']`, ~30s-resolution points) the same way other data sources are — no separate geocoding dependency needed at all, unlike the Merlin screenshot fallback above.

Also confirmed: the model in use (BirdNET+ V3.0-preview3.1) is a single unified acoustic classifier trained across 9,789 species spanning birds, amphibians, mammals, and insects (not birds-only, unlike the original free BirdNET) — today's real hike 2 export included an American Bullfrog and an Eastern Screech Owl alongside 11 actual bird species. The detection record itself carries no taxon field distinguishing these.

**Design finalized, 2026-07-29 16:59 MST (Joseph's calls):**
1. **Scope:** everything the model reports, no bird-only filtering — avoids building/maintaining our own taxon lookup.
2. **Confidence:** no code-side filter. The app's own capture-time `confidenceThreshold` (35% on today's two exports) is the only floor; Joseph will raise it to 50% in the app going forward.
3. **Output:** a table only, no narrative integration — which also means no GPS correlation is actually needed after all: with no "near the summit"-style place mention to generate, each detection just needs its UTC `timestamp` converted to local time (same `_to_local`/`format_time_local` pattern `templating.py` already uses elsewhere), not matched against the track.
4. **Columns:** Species | Count | Confidence | Time. One row per species, deduplicated (today's hike 2 alone had 27 raw Goldfinch detections) — same collapsing instinct as the log dashboard's Heartbeat convention and CARD-0109's non-redundancy rule, not one row per raw detection. **Time is the first detection** for that species (earliest `timestamp` in its group), not last or an average. **Confidence is the highest confidence value** among that species' grouped detections, not a range or average.
5. **Ingestion:** new `birdnet.py` module (matching `place_context.py`/`photo_captions.py`'s per-source pattern), reading from `<file_stem>_staging/` — same manual-staging convention Gaia's embed already uses (CARD-0112). Accepts either the bare `.json` or the whole exported `.zip` (auto-extracted), so Joseph doesn't need to unzip anything before dropping it in.

**Module architecture, 2026-07-29 17:02 MST:** `birdnet.py` sits at the same tier as `photo_captions.py`/`place_context.py`/`narrative.py` — imported directly into `generation.py` (not subprocessed like `fetch_hike_data.py`, which is shared with the interactive Skill). It **parses only** — finds the staged export, groups `detections[]` by species (name, count, highest confidence, first detection's *raw UTC* timestamp), returns structured data, does no local-time formatting and makes no API calls (no `cost_tracker` needed, unlike the other three). `run_step2()` gets one new call reading `<file_stem>_staging/`, same optional-resource treatment as Gaia's embed (empty/`None` if nothing's staged, not a failure). `templating.py`'s `render_html()` gets a new `birdnet_rows=None` parameter and owns all formatting — converts each row's raw UTC timestamp to local via the same `_to_local`/`format_time_local` helpers already used for hero stats/coverage notes, then renders the table. Keeps one time-formatting source of truth instead of three modules each doing it slightly differently. The staged export file itself is left in place after parsing (not deleted) — same as `hike_data.json`/the photos manifest, so a page can be safely re-rendered later without needing anything re-staged.

**Built and verified, 2026-07-29 17:13 MST.** New `birdnet.py` (parsing only, no API calls) plus `templating.py`'s new `birdnet_rows` parameter/table and `generation.py`'s `run_step2()` wiring, all matching the finalized architecture above. Deployed (orchestrator image rebuilt, `birdnet.py` added to the Dockerfile's `COPY` list). Verified against real production data inside the actual deployed container: staged both of today's real BirdNET Live exports (scp'd directly into each hike's `_staging/` directory — the SSHFS-Win mount referenced in CARD-0112 isn't actually set up yet, see the open question above), parsed correctly (6 species for hike 1, matching the real detection counts/confidences found during design review), rendered a clean table with correct local times — without touching either already-published live page (this adds new content, not a bug fix, so republishing needed Joseph's go-ahead first, unlike CARD-0116/0117's fixes).

**Republished both of today's live pages, 2026-07-29 17:18 MST, Joseph's go-ahead.** Scoped re-render only — no photo re-fetch, no `place_context`, no narrative regeneration, zero additional API cost: reused each page's already-persisted `hike_data.json`/photos manifest, extracted the already-published narrative paragraphs straight from the live HTML (unescaped, so `render_html` doesn't double-escape them), and added the new `birdnet_rows`. **Hike 2 gap found and fixed along the way:** it never got a persisted `PRIVATE_DIR/hike_data.json` (ran under the old single-shot pipeline, before CARD-0112's step-1/step-2 split was deployed) — re-fetched it fresh via `fetch_hike_data.py` (a plain data query, no API cost) using a window bracketing just hike 2's session (11 AM–7 PM local, wide enough to safely include the trailing-drive detection window CARD-0101 depends on, narrow enough to exclude hike 1's unrelated morning session — a full-day window was tried first and correctly rejected, since it would have re-triggered the exact multi-session-blending bug CARD-0113 fixed). Sanity-checked the refetch against the already-published, human-verified values before writing anything: session bounds/distance matched exactly (12:31 PM–2:40 PM, 2.48mi ≈ 2.5mi, trailing drive still correctly force-rejected). Now persisted to `PRIVATE_DIR` for any future re-render. **Verified live on both pages:** Wildlife Heard table present and correct on both (`hikes.jctnet.com`), hike 2's photo captions and `2026-07-29-2_photos/` paths confirmed intact (no repeat of CARD-0117's caption-loss regression).

**Related:** CARD-0073 (Hike-izer v1, Done), CARD-0074 (Hike-izer v2, has its own separate deferred-items list), CARD-0081 (HTML output — undecided yet whether bird sightings surface in Markdown, HTML, or both), CARD-0084 (Photos — same Immich-backend pattern under consideration for screenshot delivery, if that path is used), CARD-0112 (the staging-directory convention this reuses), CARD-0109 (the non-redundancy rule this table-collapsing follows), `components/hike-izer/README.md`, `components/hike-izer/fetch_hike_data.py`.

---

**Archived from `tos/kanban-board.md` on 2026-08-22 (CARD-0193)** — 15764B, over the 10000B size threshold.

### CARD-0108 · [enhancement] [hike-izer] Grounded external context for the narrative (place identification, scoped search, regional knowledge) — RESOLVED 2026-07-29 07:44 MST
**Status:** Done

**Raised 2026-07-28**, split out of CARD-0105's unscoped "answer voiced questions" and "provide history about the area" ideas after real design work and live experiments.

**Purpose, narrowed 2026-07-28 after testing:** three specific things, not "web search for general context":
1. Answer explicit questions Joseph voices during the hike and captures in Hiking Observations (e.g. today's real examples: *"what high school is it their mascot is the Bengals"*, *"wonder what that stands for"*).
2. Identify the specific named place (park, school, trail) at the hike's own coordinates.
3. Enrich with real formation-history, ownership, biology, or geology context once a place is confirmed.

**Explicitly out of scope, decided 2026-07-28:**
- General place-identification via blind web search against raw coordinates — tested directly (real Grand Rapids coordinates, Opus 4.8 + `web_search`): the tool hit a rate limit and returned zero working results across 5 attempts, still billed ~$0.35 for an *unconfirmed* guess, versus ~$0.015 for a plain no-search call that honestly said "I can't tell, here's how to check yourself." Reverse-geocoding (below) replaces this entirely, for free.
- Trail-condition / closure lookups — genuinely time-sensitive info, but by the time a hike is underway it's too late to act on; more relevant during hike *planning*, a separate concern outside this narrative-generation pipeline.

**Governing rules (carried forward from the existing `hike_start_forecast` "never fabricate absence" discipline in `SKILL.md`, and to be kept regardless of implementation):**
- Every augmented fact must come from an actual found source, not recalled from general training-data impression — omit rather than guess when it can't be confidently found.
- Every fact ties back to something that actually happened on the hike (a specific spot, a specific photo) — no disconnected trivia paragraphs. Otherwise this just trades "restates the tables" for "reads like a Wikipedia excerpt," a different flavor of the same original complaint.

**Real pipeline validated 2026-07-28, against today's actual hike coordinates:**
- **Base layer — deterministic, free, no hallucination risk.** Plain Nominatim reverse-geocoding (tested at zoom 14/16/17/18) only ever returns street/suburb-level address text — never a named park or school. **Overpass API** (a different free OSM query: "what named polygon features are near/contain this point," not "what's the nearest address") is the piece that actually works — a real query against today's coordinates correctly returned `Ottawa Hills High School` and `Ottawa Hills High School Athletic Fields` (operator: Grand Rapids Public Schools) and the separate `Ottawa Hills Park` nearby. This answers land ownership/type directly, no search or LLM guessing needed. The same mechanism (querying `route=hiking` relations) can surface named trail-network membership.
- **Enrichment layer — scoped search, only once a name is confirmed.** Targeted queries against a specific known name, not blind coordinate search — e.g. *"why is Ottawa Hills High School named that"*, *"what is Ottawa Hills High School known for (curriculum, sports records/championships)"* (both raised by Joseph 2026-07-28, using today's real sign photo as the concrete example), or *"what is [named program] Grand Rapids Parks & Rec"* once a specific public facility is identified — e.g. today's hike passed a public outdoor fitness course that appears to be a City of Grand Rapids Parks & Rec installation in 1-2 photos; once CARD-0107's vision step (or Overpass, if it's a named/tagged feature) identifies the specific program, a scoped search on that name is real value-add, distinct from guessing at unnamed equipment. This layer is the vision → search handoff described in CARD-0107.
- **Regional-scope layer — same search mechanism, keyed to the broad area, not the exact point.** Geology/topology and biology/ecology are inherently regional facts ("this area sits on glacial till from the Wisconsin glaciation," "you're in [ecoregion], where trumpet vine's presence means X") — treating them as regional rather than point-specific is both more accurate and reusable across hikes in the same area, rather than a fresh lookup every time.

**Architecture, resolved 2026-07-28 (Joseph):** no separate "Local Context" section after all — findings feed into `narrative.py`'s existing call and get woven into "The Hike" as one continuous story, same voice as the rest of the narrative. Reasoning: a separate section would need its own de-duplication pass against both the observations table and the narrative itself; folding everything into one generation call lets the same non-redundancy discipline (CARD-0109) dedupe against everything it can see in a single pass instead of needing a second reconciliation step. Also decided: the narrative should never report that a question was voiced (that's already visible in the Full Observations Log table) — it should just answer it, tied to the moment in the story.

**Built 2026-07-28:**
1. `components/hike-izer-orchestrator/place_context.py` (new) — `gather_place_context(hike_data, photos_manifest, api_key)`. Base layer: Nominatim (address) + Overpass (named park/school/trail + operator), both plain `urllib`, no new dependency. Enrichment layer: one Claude + `web_search` call, scoped only to confirmed names (from Overpass or a photo's `sign_text`, CARD-0107) and real Hiking Observations text — never a blind coordinate guess.
2. `narrative.py` — `generate_narrative()` takes a new `place_context` list, added to the JSON payload alongside `hiking_observations`/`stats`/etc.
3. `SKILL.md` part (a) — new guidance: weave `place_context` in, don't list it; answer a voiced question instead of reporting it was asked; apply the same non-redundancy discipline across `place_context`, the observations table, and the narrative's own sentences. Also fixed a stale doc reference found in passing — the weather-forecast section still described the pre-CARD-0106 "fires off the first Hiking Observation" trigger.
4. `generation.py` — calls `place_context.gather_place_context()` after photo captioning (so `sign_text` is available), passes the result into `narrative.generate_narrative()`.

**Two real bugs found and fixed via live testing against today's actual hike:**
- **Narration-leak bug.** Without a dedicated place to reason, the model's own research process ("I'll research...", "Search limit reached...") landed directly in the final text response — structurally indistinguishable from a real fact once split into lines. A live run actually discarded a **good** research pass because one narration line tripped the (correct) all-or-nothing guard. Root-fixed by adding `thinking: {"type": "adaptive"}` to the enrichment call, giving the model somewhere to reason that isn't the parsed output; the strict-format prompt instruction and the narration-keyword guard both stay as defense in depth, not the primary fix.
- **Overpass reliability, addressed 2026-07-28.** The same free public Overpass instance that worked cleanly in CARD-0107's earlier testing returned `504 Gateway Timeout` on two consecutive real runs. Added a second independent public mirror (Kumi Systems) as fallback — but a direct isolated test of that second mirror *also* timed out, on its own, nothing else running. Three timeouts across two independent providers this session reads as broadly unreliable public Overpass access during this window, not one instance having a bad day, so mirror-fallback alone wasn't enough confidence. **Fix:** up to 2 attempts per mirror (2 mirrors, 4 total attempts), raised timeouts (client 35s, Overpass's own internal `[timeout:25]`, both up from 30s/15s), 3s pause between same-mirror attempts. Re-tested afterward: succeeded in 2.3s on the very first attempt — confirms the retry/fallback code itself is correct, and that availability is genuinely intermittent (sometimes instant, sometimes down) rather than something to eliminate outright. This raises the odds of success; it isn't and can't be a guarantee against a free service with no SLA — `named_features()` returning `[]` (graceful degradation, unchanged) remains the real safety net, not this loop succeeding every time.

**Full pipeline verified against today's real hike** (base layer degraded by the Overpass timeout above, enrichment layer worked): real woven narrative produced, answering both voiced questions in-story ("The high school with the Bengals mascot is Ottawa Hills High School itself — and the 'OHHS' that prompted a puzzled aside is simply that school's abbreviation"), with genuinely surprising researched color (the school's prior "Indians" mascot and Native-American-motif building history, an NFL alumnus, the National Fitness Campaign's Dianne Feinstein-backed 1979 origin) woven into the story rather than listed. No forward-reference regression (the sensor-outage paragraph stayed clean).

**One residual issue, honestly not fully solved:** the generated narrative's opening line — "a loop of a little over two miles" — is a softer echo of the exact paraphrase-restatement problem CARD-0109 targeted (Distance: 2.0 mi is in the hero stat row). CARD-0109's rule caught 3 of the 4 original violations outright but doesn't 100%-reliably catch every instance of this shape of paraphrase. Not a new regression from this card's work, but surfaced by it -- worth a closer look at whether the rule needs another pass, or whether this is normal LLM-output variance to tolerate.

**Regional-scope layer built and verified 2026-07-28** — purpose #3 (geology/biology, keyed to the broad county/state area rather than the exact point). `gather_regional(region, cache_path, api_key)` shares the same Claude+`web_search` mechanism as the enrichment layer (extracted into a shared `_run_research_call()` helper) but keys off `_region_key()` (county/state/country from the same Nominatim response `gather_place_context()` already fetches, so no second geocoding call) and caches to a JSON file on disk — only non-empty results are cached, so a transient search failure is retried on the next hike in the region rather than permanently baked in as "nothing here."

**Two more real bugs found and fixed via this layer's live testing:**
- **Non-streaming request-length ceiling.** `_run_research_call` used a blocking `messages.create()` — a real run hit Anthropic's long-request limit at exactly 1801s and errored out (`docs.anthropic.com/en/api/errors#long-requests`): adaptive thinking plus several `web_search` round-trips can genuinely run past the ~10min a non-streaming call is allowed. Fixed: switched to `client.messages.stream()` + `get_final_message()`, which has no such ceiling.
- **Wrong text-block extraction discarded a real, good research pass.** A `web_search` response's `.content` interleaves narration-style text blocks ("I'll search for...") *before each individual search* with the real final answer at the end — joining every text block in the response (the original approach) pulled that pre-search narration in alongside good facts, correctly tripping the narration guard and discarding a real, useful pass. Fixed: only join text blocks after the *last* non-text block (i.e. after the last search result), which is the model's actual final answer.

**Real test result, 2026-07-28** (Kent County, MI, cold cache): 19 total facts — real Ottawa Hills HS history (previous "Indians" mascot, orange/black colors, 1923 land purchase, 1925 opening, Native-American-motif entrance) plus genuine regional geology (Mississippian-age Michigan Formation bedrock, Jurassic "red beds," the Plaster Creek gypsum-mining history, glacial drift landscape, county elevation range). Cache write confirmed; a second `gather_regional()` call against the same region returned instantly (0.000s) from cache, no new API call.

**Real cost measured, 2026-07-28 — higher than expected, then tuned down 70%.** The first real cold-cache run (enrichment + regional together) cost **$1.86** (300K input tokens, 9,922 output tokens, 11 web searches) — well above the ~$0.35 "expensive, rejected" blind-search figure cited above as the reason blind coordinate search was ruled out. Almost all of that ($1.50 of $1.86) was input tokens, not the web-search fee itself ($0.11) — each additional search's results compound into every later turn's input tokens within a call, so search count drives cost more than proportionally.

**Tuned 2026-07-28:** cut `max_uses` (enrichment 6→4, regional 5→3) and added explicit "budget your searches, prefer one strong source per topic" guidance to both prompts. Re-tested against the same real Kent County data, cold cache: **$0.56** (75K input tokens, 4,384 output tokens, 7 web searches — both calls used their full new budget) in **107s**, down from 21.4 minutes. Quality held up — the tuned run still produced genuinely specific, surprising facts (the Grand River as an "under-fit stream" carved by glacial meltwater, the Saginaw/Lake Michigan ice-lobe boundary, oak savanna's collapse from 2M acres to 8,000, prairie fens), not thinner or more generic than the untuned run. Regional is still a one-time-per-county cost (cached after — and for Joseph's home-turf counties, Pima/Maricopa are ~9,200 sq mi each vs. Kent County's ~870, so home-area hikes should amortize this to near-zero after the first). Enrichment's ~$0.4-0.5/hike (regional's cached share of the $0.56 subtracted) is the real recurring cost, worth watching across more real hikes.

**Cost tracking added, 2026-07-28** — `components/hike-izer-orchestrator/cost_tracking.py` (new): `CostTracker.record(response)` sums real `usage` (input/output tokens, `web_search_requests`) across every Claude call in a generation run and prices it (`$5/$25 per 1M tokens` for Opus 4.8, `$10/1,000 searches` for web search — both confirmed live against current docs). Threaded as an optional `cost_tracker=` param through `narrative.generate_narrative()`, `photo_captions.caption_photos()`, and every `place_context.py` research call; `generation.py` creates one tracker per run and appends its `.summary()` to the completion MQTT log line, so every real hike's actual API cost is now visible on the dashboard, not just discoverable via a one-off Console lookup. Verified against real API responses (`server_tool_use=None` case and a real web-search response both handled correctly) before wiring in.

**Closing note, 2026-07-29:** all three layers (base, enrichment, regional) are built, deployed, and verified against multiple real hikes, with cost tracking in place. The residual paraphrase issue above stays as documented color, not a blocker. One real accuracy gap surfaced since — `named_features()` anchors to the hike's first GPS point only, not the whole route, which can attribute a landmark from a *different* day's hike at the same starting point (found reviewing the July 29 narrative) — tracked forward as its own item in CARD-0112, not reopening this card.

**Related:** CARD-0105 (the unscoped idea this splits out of), CARD-0107 (the vision step this receives named subjects from), CARD-0109 (the non-redundancy rule this shares, and the residual issue noted above), CARD-0083/CARD-0097/CARD-0106 (the existing `hike_start_forecast` "never fabricate" pattern this design extends), CARD-0112 (the `named_features` first-point-anchor accuracy fix tracked forward there), CARD-0111 (the July 29 hike review that surfaced that gap).

---

**Archived from `tos/kanban-board.md` on 2026-08-22 (CARD-0193)** — 18619B, over the 10000B size threshold.

### CARD-0086 · [idea] [hike-izer] Automatic triggering — RESOLVED 2026-07-28
**Status:** Done

**Notes:** Raised 2026-07-23, split out of CARD-0074 (Hike-izer v2, superseded) as an individually-tracked feature. V1 is on-demand only (Joseph explicitly invokes a summary for a specific hike). This card is about detecting that a hike happened/finished and generating the summary automatically — needs a trigger mechanism decision (e.g. GPSLogger track completion, hiking-monitor's own wake/sleep pattern signaling a finished hike, a scheduled check) not yet made.

**Moved to Build 2026-07-24, now that CARD-0088 (Done) gives the M8 a real public HTTPS endpoint to receive the webhook.** Full implementation plan finalized this session — two-stage build: (1) Tasker trigger → webhook receiver on the M8, logging only, proves the field-verification risk (does GPSLogger's broadcast actually reach Tasker); (2) the generation pipeline itself (Python templating for all mechanical output + one narrow Claude API call for just narrative prose). New component: `components/hike-izer-orchestrator/`, joins `hike-izer-web`'s compose project, reachable at `https://photo-server.tailfe828a.ts.net/webhook/hike-end` via a new Caddy `reverse_proxy` route (no second Funnel port). Shared-secret query-param auth, same pattern as the existing Apps Script `key=`.

**Day/date handling — never assume Arizona.** A hike could happen anywhere Joseph is carrying his phone, not just at home, so (caught before build started) the webhook payload carries the phone's own local date/time and UTC offset (Tasker's local-clock variable) rather than the receiver inferring or hardcoding Arizona time. Every rendered timestamp in the output is presented in that local time and explicitly labeled as local.

**Requires Joseph:** create an Anthropic API key (Claude Console — account/billing action, can't be done on his behalf; confirmed 2026-07-24 none exists yet), build the Tasker profile on his phone, trigger a real GPSLogger stop to prove the trigger fires end-to-end.

**Stage 1 (trigger + connectivity) built and verified 2026-07-24.** New component `components/hike-izer-orchestrator/` (`app.py`, stdlib-only, matching `fetch_hike_data.py`'s convention) deployed as a second service in the `hike-izer-web` compose project on the M8 — shares that project's Docker network, reachable from Caddy by service name, no second Funnel port. Caddyfile got a `handle /webhook/*` route proxying to it. Shared-secret auth via `?key=` query param (`WEBHOOK_SECRET`, generated fresh, in `credentials.local.md` + the M8's `.env`). Verified live against the real public URL (`https://photo-server.tailfe828a.ts.net/webhook/hike-end`): correct secret + `stopped` event → 200, logged; wrong/missing secret → 401; `started`/`fileuploaded` → 200, logged-and-ignored. Existing static-file listing on the same domain confirmed still working after the Caddyfile change. `photo-server-heartbeat.py` extended to check `hike-izer-orchestrator`'s container health too (own `HEALTHCHECK`, `python3`/`urllib` against `/health` — no extra package); installed on the M8 and confirmed via a real run: `Heartbeat sent. status=online`.

**Payload contract simplified 2026-07-24, after checking Tasker's actual variables (not guessed).** `%TIMES` is Unix epoch seconds, not a local-time string — confirmed via `hiking-monitor-claude-code-instructions.md` Step 24 and Tasker's own docs. Rather than three separate local_date/local_time/utc_offset fields (error-prone to string-concatenate correctly in Tasker's UI), the webhook now takes one `local_datetime` field: a full ISO 8601 string with UTC offset (e.g. `2026-07-24T14:32:10-07:00`), built by Tasker's "Parse/Format Date and Time" action (`yyyy-MM-dd'T'HH:mm:ssZZ`, Joda-Time format) — one unambiguous field, parseable via Python's `datetime.fromisoformat`. Full Tasker profile build steps (Task + Event/Intent-Received Profile, mirroring CARD-0007's HTTP POST pattern) written into `components/hike-izer-orchestrator/README.md`.

**Stage 1 fully verified end-to-end 2026-07-24, real phone event confirmed.** After the adb decisive test (above) proved Tasker/Android broadcast reception works fine, a subsequent real Start/Stop in GPSLogger's own UI finally produced a real broadcast — received cleanly: `{"gpsloggerevent": "stopped", "filename": "20260724", "startedtimestamp": "1784939105391", "duration": "8", "distance": "0.0", "local_datetime": "2026-07-24T17:25:14-07:00"}`. Whatever was blocking GPSLogger's broadcast in the earlier attempts (root cause not conclusively identified — possibly transient app/service state, possibly related to enabling GPSLogger's own debug-to-file logging in between attempts) resolved itself; the full chain (GPSLogger → Tasker Task → webhook → orchestrator) is now proven working with a genuine phone-triggered event, not just synthetic curl/adb tests. Stage 1 is done-done. Stage 2 (generation: Python templating + the narrow Claude API call) not started.

**Stage 2 (generation) built and verified end-to-end 2026-07-24.** New modules in `components/hike-izer-orchestrator/`: `templating.py` (mechanical Markdown/HTML builder, direct port of `html-template.html`'s field mapping — unit-tested locally against both the `hike_confirmed: true` and `false` paths, plus a photos-manifest case), `narrative.py` (one Claude API call, `claude-opus-4-8`, `messages.parse()` + `output_format` for structured `{"narrative_paragraphs": [...]}`, system prompt built from the deployed `SKILL.md` copy read at call time), `generation.py` (orchestrates `fetch_hike_data.py`/`fetch_hike_photos.py` as subprocesses, then templating + narrative, writes straight into `srv/`), `mqtt_log.py` (`jctsh/hike-izer/publish/log`, same connect/publish pattern as `photo-server-heartbeat.py`). `app.py` now runs generation in a background thread so Tasker's HTTP timeout doesn't fire while the pipeline (which can run well past 10s) is still working. Orchestrator switched from a bind-mounted stock image to a real `Dockerfile` build (needs `anthropic`/`paho-mqtt`, unlike stage 1's stdlib-only design); `srv` volume changed to read-write for the orchestrator so generation writes directly into what `web` serves, no `scp` step.

**Real bug found and fixed during verification: query-window boundary.** Initial local-testing against a real day (2026-06-18, chosen because a reference doc already exists to compare against) surfaced a genuine discrepancy — a wrong first attempt (mis-diagnosed, then correctly fixed) around whether the fetch window should be bounded by the hike's own local UTC offset or a hardcoded `Z`. Resolved: **bounding by the hike's own local offset is correct** — it's what makes SKILL.md's manual "does this GPS session actually belong to the previous local day" judgment call unnecessary in the automated path, since a session that only *looks* cross-midnight in UTC-Z terms (e.g. an evening session in a DST-observing location) is naturally excluded by a correctly-local-bounded window, no reattachment logic needed. Confirmed live: New Mexico trip data (real coordinates near Elephant Butte/Truth or Consequences — Claude's narrative correctly named the real location from GPS coordinates, not a hallucination) using the correct `-06:00` MDT offset (New Mexico observes DST, unlike Arizona) correctly included only the true local-June-18 session and excluded the prior evening's, matching the interactively-authored reference doc's own manually-curated result — with the added benefit that the automated version required no manual curation to get there.

**Second bug found and fixed:** the coverage panel's "gaps over 6 minutes" note rendered raw UTC timestamps instead of local time — missed in the initial build despite the observations table already converting correctly. Fixed (`templating.py`'s `coverage_notes()` now takes `offset_delta`/`offset_str`), verified via a targeted test.

**Verified live on the M8, 2026-07-24:** real webhook-triggered generation for 2026-06-18 (correct offset) produced accurate Markdown/HTML matching the reference doc's actual content (improving on it — correctly excludes the misattributed evening session), with photos correctly fetched and embedded, published to the live public URL (curl confirms `200`). This intentionally overwrote the live `2026-06-18_hike-summary.html` (originally from CARD-0083/CARD-0088's interactive generation) — kept, by Joseph's call, since the new version is more accurate.

**MQTT publish-visibility logging confirmed live 2026-07-24.** `hike-izer-orchestrator` Mosquitto account created on the Pi (`sudo mosquitto_passwd`, credentials in `credentials.local.md`). Test publish confirmed landing on the log dashboard (`http://raspberrypi.local`) — `hike-izer-orchestrator` now appears as its own component filter, `System`-category test message received and timestamped correctly in MST. Both stages of CARD-0086 are now fully built and verified end-to-end — nothing left blocking except real-world usage over time.

**Real GPSLogger stop never reached Tasker — extensive live debugging 2026-07-24, root cause narrowed but not yet fixed.** Joseph built the Tasker Task (`Hike-izer Webhook`: Date/Time Format action → `local_datetime`, then HTTP Post) and Profile (`Intent Received`, action `com.mendhak.gpslogger.EVENT`) correctly — confirmed via screenshots. A real Start/Stop in GPSLogger's Simple View produced zero events in Tasker's Run Log (confirmed clean — Run Log logging was verified actively recording other activity at the time). Ruled out, in order: wrong stop control (Joseph used GPSLogger's own Start/Stop Logging button, the correct one), Tasker battery restriction ("Allow background usage" already on), outdated Tasker (6.6.20, confirmed current), wrong/outdated GPSLogger (version 135, Dec 2025, matches current source). **Decisive test:** installed `adb` (Android platform-tools, via winget) on Joseph's Windows machine, connected his phone via USB (debugging authorization required several retries — stale auth state, needed "Revoke USB debugging authorizations" + reconnect with screen already unlocked before plugging in), then ran `adb shell am broadcast -a com.mendhak.gpslogger.EVENT --es gpsloggerevent stopped ...` to send the exact broadcast GPSLogger would send, from a genuinely different process. **This worked end-to-end** — Tasker caught it, ran the Task, POSTed correctly, receiver logged a clean `stopped` event with correct `local_datetime`. This proves the entire chain (Tasker → webhook → orchestrator) is correct and that Android/Tasker can receive cross-app broadcasts fine on this phone — the remaining problem is narrowly that **GPSLogger itself isn't sending the broadcast** when Start/Stop Logging is tapped in its Simple View screen, despite `GpsLoggingService.java`'s `notifyByBroadcast()` being unconditional in the reviewed source. Next diagnostic step: check whether GPSLogger has its own debug/view log (would show whether it even attempted the broadcast) and whether Simple View's controls go through the same code path as the full app.

**Blocking dependency, corrected 2026-07-24 (same stale carry-over already fixed on CARD-0084):** CARD-0074's blanket "hiking-monitor device needs to be operational" note doesn't actually apply here either — triggering off GPSLogger or the Hiking Observations pipeline is phone-based, entirely independent of the ESP32 hiking-monitor device. No real blocker.

**Candidate trigger mechanisms considered 2026-07-24, while scoping CARD-0083's sibling need (hike-*start* detection):**
- **Explicit "end of hike" phrase match** (via the Hiking Observations pipeline, CARD-0007) — real precedent exists (today's actual hike literally ended with the observation "end of hike"), but depends on Joseph remembering to say it every time; if he doesn't, the trigger silently never fires.
- **GPS-session-based absence detection** (no new GPS point for N minutes) — doesn't depend on Joseph saying anything, but needs polling/a scheduled check rather than a clean event, has inherent lag (waiting out the N-minute window), and risks a false positive from a temporary GPS dead zone (canyon, dense tree cover) that isn't really the hike ending.

**Leading candidate, found 2026-07-24 — GPSLogger's own native start/stop broadcast, via Tasker.** GPSLogger (the Android app already used for the GPS Track pipeline, `com.mendhak.gpslogger`) broadcasts a native Android event whenever logging starts or stops (`com.mendhak.gpslogger.EVENT`), confirmed via its own documentation: *"GPSLogger sends a broadcast start/stop of logging... which you can receive as an event"* in Tasker. This beats both candidates above:
- Fully automatic — tied to something Joseph already reliably does (stopping GPSLogger when the hike ends), not a new verbal habit to remember.
- Immediate, no lag — fires the instant logging actually stops, not after an inferred timeout, and no false-positive risk from a temporary signal dead zone.
- Reuses proven infrastructure, not new territory — Tasker already fires an HTTP POST to the same Apps Script for the "Log Observation" task (CARD-0007, Steps 24-26 in `hiking-monitor-claude-code-instructions.md`). This would be a second, small Tasker profile copying that same pattern: trigger on "Intent Received" for GPSLogger's stop broadcast, action: HTTP POST to a hike-end endpoint.

**Confirmed 2026-07-24 (GPSLogger source + FAQ):** action `com.mendhak.gpslogger.EVENT`, extras `gpsloggerevent` (`"started"`/`"stopped"`/`"fileuploaded"` — one action string for both, distinguished by this field), `filename`, `startedtimestamp`, `duration`, `distance`. Tasker branches on `%gpsloggerevent` = `stopped`; `started`/`fileuploaded` logged and ignored.

**Invocation architecture — how the trigger actually produces a summary, researched 2026-07-24.** The trigger above only gets an HTTP POST to *some* endpoint when a hike ends. That endpoint has to actually run Hike-izer's narrative-writing step (currently `.claude/skills/hike-izer/SKILL.md`, only ever invoked by Joseph inside an interactive Claude Code session) with nobody watching. That's the real "does this need an agent?" question, and the answer is **no, not in the heavyweight sense** — reasoning below.

Note on "Cowork": not a current Anthropic product name I recognize — didn't want to fabricate an answer around it. The real menu of options for calling Claude programmatically:
- **Claude API — a single Messages API call** (or a short tool-use loop). You write the plumbing; Claude answers one well-scoped request.
- **Claude API + tool use (Tool Runner or manual loop)** — for when Claude itself needs to decide what to fetch/call across multiple steps.
- **Claude Agent SDK** (`claude-agent-sdk`) — Claude Code itself, packaged as a library: built-in Read/Write/Edit/Bash tools, the full agent loop, hooks, subagents. The closest thing to "run the Hike-izer Skill unattended, exactly like Joseph running Claude Code interactively."
- **Managed Agents (Claude's hosted agent platform)** — Anthropic runs the agent loop *and* hosts the sandbox container the agent's tools execute in; you create a persisted Agent config once, then fire Sessions (event-driven, or on a cron schedule via Deployments).

**Recommendation, refined 2026-07-24: skip the agent frameworks entirely and use one narrow Claude API call.** Hike-izer's actual pipeline (`fetch_hike_data.py` → `fetch_hike_photos.py` → write Markdown/HTML) is already a deterministic sequence of existing Python scripts. **New realization:** most of the *output* itself is also fully mechanical — the hero stat row, data tables, full observations log, coverage panel, and weather forecast cards are all direct field-to-template mappings with zero creative judgment. Only the narrative story (SKILL.md step 4a) genuinely needs an LLM. So the build splits cleanly: a Python templating module handles 100% of the mechanical output (a direct port of `html-template.html`'s existing mapping), and **one** Claude API call (`claude-opus-4-8`, structured output via `output_config.format`) returns just `{"narrative_paragraphs": [...]}`. Smaller, cheaper, more reliable than asking Claude to produce full HTML — still squarely the "classification/summarization/extraction" tier, not agent territory.

**Where this orchestrator lives — ties to CARD-0088 (Done).** Reuses CARD-0088's existing Tailscale Funnel URL via a new Caddy `reverse_proxy` route (`/webhook/*`) rather than exposing a second port — see the architecture note above.

**Logging/MQTT visibility becomes possible here, not before (noted 2026-07-24 during CARD-0088 build).** CARD-0088 explicitly skips a per-publish MQTT log line for its `scp`-based deploy step, because that step runs on Joseph's Windows machine, which has no MQTT-publish capability and adding one would mean a new pip dependency for a one-off script. Once this card's orchestrator exists and lives on the M8 (a Linux host with local broker access, same as every other JCTsh MQTT publisher), that constraint goes away — the orchestrator is the natural place to add real dashboard visibility (`jctsh/hike-izer/publish/log` on a successful auto-generated summary, `Alert`-category on a failure) using the same pattern already established for the backup script and scheduled-reboot notifications. Built in from the start here.

**Real-world usage confirmed repeatedly since 2026-07-24 — closing out.** The card's own 2026-07-24 status already said "nothing left blocking except real-world usage over time"; it just never got formally moved. Since then: CARD-0092 (calendar home page) was closed specifically on "automatic path confirmed on a real hike," and the entire 2026-07-28 Ottawa Hills hike (CARD-0107/CARD-0108/CARD-0109 testing) ran through this exact automatic GPSLogger → webhook → orchestrator pipeline with no manual invocation.

**Related:** CARD-0073 (Hike-izer v1, Done), CARD-0074 (superseded), CARD-0083 (sibling trigger-mechanism need, opposite end of the hike, Done), CARD-0007 (Hiking Observations pipeline — the phrase-match fallback and the Tasker HTTP-POST pattern the leading candidate copies), CARD-0088 (hosting, Done — this card's webhook route lives on that Funnel URL), `components/hiking-monitor/gps-pipeline.md` (GPSLogger app details), `components/hike-izer/fetch_hike_data.py`, `.claude/skills/hike-izer/SKILL.md` (the narrative-writing instructions this card's orchestrator calls Claude with).

---

**Archived from `tos/kanban-board.md` on 2026-08-22 (CARD-0193)** — 16167B, over the 10000B size threshold.

### CARD-0073 · [idea] [hike-izer] Hike-izer — narrative summary application layer for hiking data — RESOLVED 2026-07-18
**Status:** Done

**Notes:** Raised 2026-07-18. JCTsh's hiking-monitor pipeline already covers data collection (ESP32 sensors — BME280, LTR-390 UV; GPS track via Pixel GPSLogger) and data storage (GPS Track and Environmental Data Google Sheets, per CARD-0020). Hike-izer adds the missing layers on top: a controller layer (rules/analysis) and a rudimentary presentation layer, turning raw hike data into a narrative story of the hiking event rather than just charts.

**Relationship to CARD-0020:** originally complementary, not competing (Joseph's call, 2026-07-18) — CARD-0020's Looker Studio dashboard scoped to raw charts/maps, Hike-izer's output the narrative layer, both from the same Sheets data. **Update 2026-08-02:** Hike-izer's own visuals (CARD-0082 Route Map, CARD-0110 Elevation & Speed chart, CARD-0133 event markers) grew well past "narrative layer only" and now cover CARD-0020's original single-hike scope better than a generic dashboard chart would — CARD-0020 rescoped to a cross-hike/aggregate view instead (season trends, cumulative route map, sensor health over time), which is still genuinely complementary since Hike-izer has no memory across hikes.

**Data source inventory (2026-07-18):** confirmed against the real pipeline code/docs, not assumption —
- **Environmental Data sheet** — full column set is A–Z, not A–X as `data-pipeline.md` previously (incorrectly) documented; `illuminance_lx`/`solar_v` were missing from that doc, now fixed.
- **Hiking Observations sheet — exists and works.** There IS a capture mechanism (Tasker voice-widget → Apps Script → auto-categorized row), built and field-confirmed in Steps 23–26.
- **GPS Track sheet** (via GPSLogger, not GaiaGPS — GaiaGPS was the superseded original plan and several docs still stale-reference it) — includes `alt` (altitude), which covers "height" without new hardware.
- **Timeline sheet** — existing manual-menu action already merges Environmental Data + Hiking Observations into one time-sorted sheet.
- **Compass/heading** — still a real gap, no sensor and not derivable cleanly from GPS Track alone (direction of travel ≠ which way the hiker was facing). Deferred, see scope below.
- Confirmed target trip for prototyping: the **2026-06-15 two-week trip**. GPS pipeline was redeployed 2026-06-12 (before the trip) so GPS Track data is very likely present; observations pipeline's exact completion date relative to June 15 isn't pinned down in the docs — check the real sheet before assuming observation rows exist for this window.

**Build step taken (2026-07-18):** extended `core/data-pipeline/environmental-data.gs` with a new read-only `action=export` on `doGet` — returns any sheet (Environmental Data / Hiking Observations / GPS Track) as JSON, optionally filtered by an ISO 8601 timestamp range, reusing the existing API_KEY auth. Documented in `data-pipeline.md`.

**Deployment saga (2026-07-18) — resolved.** The normal "Manage deployments → pencil → New version → Deploy" flow silently failed three times in a row: the Active deployment kept serving old code with zero errors, no indication anything was wrong. Root-caused by adding a `SCRIPT_VERSION` constant returned in every `doGet` response (including the "unknown action" fallback) — confirmed via `Deploy → Test deployments` (`/dev` URL, always runs Head/latest-saved code) that the *saved code* was correct the whole time; the problem was isolated specifically to the versioning step not promoting to the Active deployment. Fix: created an entirely new deployment (`Deploy → New deployment`) rather than continuing to fight the existing one — worked immediately, confirmed via `action=version` and a real `action=export` call that pulled all 6,202 real Environmental Data rows for the June 15 trip window.

**New deployment URL migrated everywhere the old one was referenced:** `credentials.local.md`, `data-pipeline.md`, Node-RED's `APPS_SCRIPT_URL` (found to live in `/home/pi/.node-red/environment`, a systemd `EnvironmentFile` — **not** the Node-RED editor's Settings → Environment Variables panel, which only covers flow/global-scoped vars, not OS-level ones; updated via SSH over Tailscale, `nodered` service restarted, verified via the live process's own `/proc/<pid>/environ` that the new URL is actually loaded, not just written to disk), and GPSLogger's custom URL setting on the Pixel (Joseph updated manually). Old deployment now fully unreferenced — left in place, harmless if idle.

**v1 scope — locked 2026-07-18 (Joseph's call, no separate Phase 1 planning doc; this card is the plan, given the scope's size — revisit if Hike-izer grows past v1 into photos/weather/automation):**
- **Mechanism:** a Claude Skill.
- **Trigger:** on-demand, with the **date/date-range as a prompt parameter** — not hardcoded to June 15. June 15 is the first test case, not the only one; Joseph may want to run this against other hikes later.
- **Inputs:** Environmental Data + Hiking Observations + GPS Track (lat/lon/alt), pulled for the requested date range via `action=export`.
- **Computed:** sun position (from lat/lon + timestamp), using GPS `alt` for height.
- **Output:** one Markdown document per run, including:
  1. A narrative story of the hike.
  2. Data tables/summaries as appropriate.
  3. **An explicit "expected vs. actual" data-coverage section** — not just an aside, a first-class part of the output. What was expected (sensor readings at ~2-min cadence, one observation stream, GPS trackpoints every ~30s, over the requested date range) versus what's actually present. This is meant to double as a health check on the whole hiking-monitor pipeline every time a summary is generated.
- **Explicitly deferred, not forgotten:** photos (Immich integration unbuilt), historical weather (no source picked), compass/heading (no data source), automatic triggering, rendered web page output.

**v1 built and verified end-to-end (2026-07-18).** Built as a real Claude Skill: `.claude/skills/hike-izer/SKILL.md` (instructions: determine date range, read credentials from `credentials.local.md`, run the fetch script, write the narrative in three parts, save to `hike-izer/summaries/`) plus `components/hike-izer/fetch_hike_data.py` (stdlib-only Python: fetches all three sheets via `action=export`, computes coverage stats, computes sun position via the NOAA/Meeus solar algorithm sampled along the GPS track). Code and generated output deliberately kept apart: code under `components/hike-izer/`, results under the top-level `hike-izer/summaries/` — so a future HTML presentation layer doesn't end up mixed in with source.

**Two real bugs found and fixed by testing against the real June 15 trip data, not left on paper:**
1. GPS Track's actual columns are `accuracy_m`/`altitude_m` (confirmed in `gps-pipeline.md`) — the script initially used `acc`/`alt`, silently returning `None` for every altitude. Fixed.
2. The GPS Track coverage calc originally compared point count against a full-date-range 30-second-cadence expectation, which produced a misleading "~1% coverage" — GPSLogger only runs during actual hikes, not continuously across a multi-day camping trip. Replaced with session detection (splits on >10min gaps, reports per-session coverage) — correctly showed 2 real sessions at 85.9% and 95.6% coverage instead of one alarming, wrong number.

**First real output generated:** `hike-izer/summaries/2026-06-15_hike-summary.md`. Genuine findings surfaced by the coverage section (not fabricated for demonstration) — UV index never exceeded 0.1 across the whole trip despite 110°F+ heat, consistent with the LTR-390 validation concern already tracked in CARD-0065; battery voltage dropped to 2.85V, lining up with the documented field LiPo failure (`README.md`, 2026-07-03); zero Hiking Observations rows despite the pipeline being confirmed working, worth a conscious check on whether the voice widget just wasn't used; only 2 GPS sessions (~4.3 hours total) detected across the full 2-week window. This is exactly the "awareness of how components are operating" goal from the original ask — not hypothetical, it found real things on the very first run.

**Second test round (2026-07-18), deliberately an edge case: today's date, still in progress.** Found and fixed two more real bugs:
3. **Future-window truncation.** Requesting a window extending into the future (e.g. today, before the day is over) computed "expected readings" against the full nominal window, producing a misleadingly low coverage % that wasn't a real problem. Fixed: `analyze_coverage` now caps the expected-calculation window at the actual current time when the requested end is in the future, and the output JSON carries `window_truncated_to_now` so the narrative can say so explicitly. `SKILL.md` updated to call this out when true.
4. **Cross-sensor contamination — more serious, and retroactive.** The Environmental Data sheet is shared across every JCTsh environmental sensor (`source` column distinguishes them), and the fetch script wasn't filtering by it. Testing today's date turned up 270 rows that were **entirely** `front-porch-temp-sensor`, not hiking-monitor — which would have been reported as hiking-monitor activity. Re-checked the already-published June 15 summary against this: **4,270 of its original 6,202 "hiking-monitor" rows were actually front-porch-temp-sensor.** Added a `--source` filter (default `hiking-monitor`) to `fetch_hike_data.py`, defaulting out any other source and reporting what else was seen but excluded. **`hike-izer/summaries/2026-06-15_hike-summary.md` regenerated with corrected numbers** (coverage 57.4%→17.9%, temp/humidity ranges narrowed, and a real ~10-hour Environmental Data gap on June 17 surfaced that had been masked by the other sensor's readings filling the same time slots) — the file carries its own correction note.

**Second real output, and a genuinely different kind of finding:** `hike-izer/summaries/2026-07-18_hike-summary.md`. Zero hiking-monitor Environmental Data readings all day, despite 7 Hiking Observations and 1 GPS point clearly showing a real walk happened (phone-based logging, no device). Since the same shared sheet has 270 `front-porch-temp-sensor` readings for the same day, this points at the hiking-monitor device itself (not deployed/charged/powered on?), not the pipeline — a different, useful class of signal than the coverage-percentage findings from the June 15 run.

**Model + presentation refinement (2026-07-18, Joseph's direction):**
1. **Feet is now the primary (only) unit for elevation** — `fetch_hike_data.py` converts `altitude_m` to `alt_ft`/`stats.altitude_ft` internally; the raw meters value from the sheet is never surfaced in output. Added a `compute_stats()` function to the script so temp/humidity/pressure/UV/battery/altitude ranges are computed once, consistently, rather than re-derived ad hoc each time.
2. **A hiking event is now defined as a single calendar day**, even a multi-day trip is a *series* of single-day events, not one combined report. `SKILL.md` rewritten around this: fetch the full requested range once, identify which individual days have real activity (a GPS session in practice), generate one `<date>_hike-summary.md` per active day. **Edge case handled:** a GPS session crossing UTC midnight (June 17 23:51 → June 18 02:59, confirmed real) is attributed to the day it *started*, not split into two partial, confusing day-summaries — verified by testing that querying June 17 alone only caught the first 8 minutes of that session.
3. **Non-redundant narrative rule added to `SKILL.md`:** the data table carries the numbers; the narrative synthesizes/interprets/draws conclusions rather than restating figures already sitting in a table two sections down.

**Summaries restructured to match:** the old combined `2026-06-15_hike-summary.md` (spanning the whole ~2-week query range) removed, replaced with `2026-06-17_hike-summary.md` and `2026-06-18_hike-summary.md` — the two actual hiking days, computed by partitioning the already-fetched dataset (Environmental Data bucketed by calendar day, GPS sessions attributed by start day) rather than re-querying per day, exactly as `SKILL.md` now prescribes. New finding from the properly-split data: **battery voltage declined day-over-day (3.97–4.36V on June 17 → 3.00–4.00V on June 18)** — a real trend, not noise, and an earlier data point in the same LiPo failure this trip is already known for.

**Hike-vs-not-a-hike classification added (2026-07-18, Joseph's direction).** Raised directly by the June 17 session: Joseph doesn't hike at night, so a gap-based GPS cluster alone isn't good enough evidence that a hike happened — it could be a drive, camp-site GPS drift, or (worth naming, since it's the actual constraint) genuinely happening in the dark. Added `_classify_hike()` to `fetch_hike_data.py`: every candidate GPS session must pass **both** a daylight check (≥80% of points at civil twilight or brighter, sun elevation > -6°) and a walking-pace check (median point-to-point speed 0.15–3.0 m/s / ~0.3–6.7 mph, computed via haversine distance) to count as `is_hike: true`. Sessions that fail carry `rejection_reasons` rather than being silently dropped — including a distinct "insufficient data" reason for single-point sessions (a session with 1 point has no speed pairs to compute; defaulting that to "0 m/s = stationary" would overstate what's actually known, so it gets its own honest reason instead). `coverage.gps_track.hike_confirmed` is `true` if any session that day passes both checks.

**Verified against real data both directions:** the two known-real hikes (June 17 evening, June 18 morning) both correctly classify `is_hike: true` (June 17's median speed came out to a low-but-passing 0.15 m/s — right at the threshold, consistent with a stop-and-start evening hike, not a red flag). July 18's lone GPS point correctly classifies `is_hike: false` with the new "insufficient data" reason rather than a misleading "stationary" one.

**`SKILL.md` updated with a new "What counts as a hike" section and an explicit "unable to confirm" output path** — when `hike_confirmed` is `false` for a requested day, the Skill must say so plainly and explain why (using the real `rejection_reasons`), rather than writing a normal hike narrative or silently producing nothing. `hike-izer/summaries/2026-07-18_hike-summary.md` rewritten to follow this path properly, now citing the actual classification output instead of the ad hoc framing used before this logic existed.

**Related:** `components/hiking-monitor/gps-pipeline.md`, `components/hiking-monitor/data-pipeline.md`, `components/hiking-monitor/hiking-logger.md`, CARD-0020 (Looker Studio dashboard, complementary).

**Resolution (2026-07-18):** closing as **version 1 done**. Core loop fully built and verified against real data, not left on paper: fetch → source-filter (`hiking-monitor` only) → hike classification (daylight + walking-pace checks, honest "unable to confirm" path when nothing passes) → single-day narrative with non-redundant tables and an explicit expected-vs-actual coverage/health section, elevation in feet throughout. Along the way, found and fixed real bugs — wrong GPS column names, misleading multi-day coverage math, future-window truncation, and cross-sensor contamination that had silently corrupted the first published summary — exactly the "awareness of how components are operating" goal from the original ask, working in practice, not hypothetically. Remaining ideas (photos, historical weather, hiker's compass/heading, automatic triggering, rendered HTML output) are real but represent v2, not blockers on calling v1 done — split out to **CARD-0074**, which also needs the hiking-monitor device itself operational again (it produced zero readings on 2026-07-18) before it can be built against fresh real data.

**Closed 2026-07-18 — Joseph directed the close.**

---
