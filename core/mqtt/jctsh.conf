# /etc/mosquitto/conf.d/jctsh.conf
# Deployed alongside the default mosquitto.conf.
# Adds ISO 8601 timestamps — required for fail2ban log parsing.

log_timestamp true
log_timestamp_format %Y-%m-%dT%H:%M:%S
