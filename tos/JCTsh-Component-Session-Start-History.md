# JCTsh-Component-Session-Start.md — Version History

Full changelog for `JCTsh-Component-Session-Start.md`, split out (CARD-0292) alongside
the analogous split for `JCTsh-Operating-System.md` — same growing "Prior version
description" chain pattern, applied proactively here before it grew as large. Read on
demand only, not part of routine reading. The live document's header now states only
the current version and its own description.

---

| Version | Description |
|---|---|
| 1.21 | Component/Cluster Registry row flipped from `No`/-- to `Yes`/Active. Step 3's reconciliation check found the intended scope (`automatic-garage-door-opener-closer`, `garage-presence`, `garage-radar`, `salt-sensor`) matches the existing row exactly, so no scope change was needed. Startup notes worth keeping: the row's `salt-sensor` membership was itself only added earlier the same day (2026-09-25, Joseph), so this is the first initiation of the merged four-component scope; the cluster has no kanban-tagged cards for `automatic-garage-door-opener-closer` at all (its work rides on CARD-0260 `[homeassistant]` and CARD-0164 `[architecture]`), and nothing sits in Build. |
| 1.20 | Network/infra-visibility cluster initiated, 2026-09-23 (Joseph: "component session start up for network/infra-visibility cluster") -- Component/Cluster Registry row flipped from `No`/-- to `Yes`/Active. Step 3's reconciliation check found the intended scope (`netalertx`, `core/mqtt`, `core/logging`, `core/node-red`) matches the existing row exactly, so no scope change was needed. Startup notes worth keeping: this cluster's four covered directories are all runtime-visible rather than hardware -- `core/node-red` has no `README.md` (its `watchdog-README.md` stands in, and `CLAUDE.md` is an empty stub), and `core/mqtt`/`core/logging`/`components/netalertx` `CLAUDE.md` files are all stubs. No `.claude/skills` operating doc exists for any covered component. Open cluster cards at initiation: CARD-0328 and CARD-0324, both Backlog; nothing in Build/Planning/Design. |
| 1.19 | Added component-only step 3, 2026-09-22 (Joseph's direct go-ahead, from a hike-izer cluster session) — read each covered component's own *operating* doc in full, above all `.claude/skills/<name>/SKILL.md`, plus any procedural sibling runbook a `README.md` points at, before operating that component. Closes a real structural hole, not a theoretical one: `SKILL.md` lives outside `components/<name>/`, so steps 1–2 (both `README.md`s and both `CLAUDE.md`s read, startup otherwise correctly executed) never surfaced hike-izer's standing rule that `generation.py --step2`'s `--narrative` flag is opt-in-only and needs Joseph's explicit per-hike approval every time (~$0.5–0.8/hike vs ~$0.06) — with a generic "run step 2" named in that same rule as specifically *not* permission. Asked to "do the second pass for today's hike," the session ran `--narrative` anyway and was stopped mid-run by Joseph, who noted it was a repeat offense. Step 3 also states the generalizable half: where an operating doc names a default for an ambiguous request, that default governs, and ambiguity never resolves toward the costlier or less reversible option. Step 3 of the Initiating procedure updated from "two component-only read steps" to "three." |
| 1.18 | Ops cluster initiated, 2026-09-22 (Joseph: "component session start up for ops") — Component/Cluster Registry row flipped from `No`/— to `Yes`/Active. The first of the 2026-09-18 batch of *predefined, proposed* clusters to actually be started, and the first initiation to follow the Initiating/Rebuilding procedure as literally written (1.15 was an in-place re-scope of a running session; 1.17 was driven by a new component directory appearing). Step 3's reconciliation check found the intended scope and the recorded scope already identical — `core/maintenance`, `core/docker`, `hosts/m8`, `hosts/pi1` — so no drift to resolve, which is itself the first real evidence that predefining groupings ahead of use does the job it was added for. |
| 1.17 | Porch/patio temp sensors cluster initiated, 2026-09-21 (CARD-0219, Joseph) — the Component/Cluster Registry's single-component `front-porch-temp-sensor` row merged with `back-patio-temp-sensor` into one `porch/patio temp sensors` row, Initiated/Active. First registry change driven by a *new* component rather than a re-scoping of existing ones, and the first merge of two rows rather than a flip or an addition. Rationale recorded on the row's own bullet: back-patio is defined by its card as a duplicate of front-porch's design (same board, sensors, I2C pins, ESPHome pattern, MQTT/Node-RED/Sheets path), so findings travel between them by construction, not by the judgment call the 2026-09-18 proposed groupings rested on. Two prior statements amended in place rather than deleted, per the Documentation Structure "mark and strike through" rule: the "self-contained single-component clusters" bullet (front-porch struck) and the CARD-0294 tag-reconciliation note's "planned components with no directory yet" list (back-patio struck) — the latter now also carrying the exception that made it wrong, that `JCTsh-Component-Planning-Pattern.md`'s clone path creates a component's directory at Planning, not Build. |
| 1.16 | Step 2 now names `/rename` explicitly as the mechanism (confirmed against Claude Code's own docs, not assumed) instead of just describing its effect. Added two new call-outs, prompted by Joseph asking directly how `/clear` interacts with a named component session and how effective the whole scoping scheme actually is: `/clear` keeps the `/rename`d name on the new post-clear conversation but wipes everything the session had actually read/internalized, so a cleared session is findable but not still scoped without re-running startup; and an explicit "no technical enforcement, this is discipline, not a restriction" limitation, since naming/the Registry only make scope discoverable and checkable at initiation, never continuously enforced. |
| 1.15 | Hiking-monitor cluster actually initiated, 2026-09-20 — Component/Cluster Registry row flipped from `No`/— to `Yes`/Active. Not via the clean "open a new terminal" procedure the Initiating/Rebuilding steps describe: Joseph directly asked an already-running hike-izer session to re-scope itself mid-conversation to pick up CARD-0259's Planning work. Documented as a real, messier variant of initiation (startup steps re-run in-place, scoped to `hiking-monitor`+`air-quality-monitor`) that the existing procedure text doesn't explicitly cover yet. |
| 1.14 | CARD-0315's third interim call (Joseph, from a LogSeq-scoped component session asking to run this document's startup) — added `LogSeq`, `PB-Blog`, and `Rethinking Scripture Bible Study` as Component/Cluster Registry rows: whole sibling repos under `Projects/`, not jctsh-internal `components/`/`core/`/`hosts/` directories, same single-"component" shape as `tos`/`architecture` one level up. Only `LogSeq` marked Initiated. Documented, not resolved: the 9-step table's steps 1/2/3/5/6/9 assume jctsh's own shared `kanban-board.md`/git tree, so scoped to one of these three repos they point at that repo's own board/tree instead (a different card format, no component tags to scope by for LogSeq/PB-Blog); steps 4/8 (jctsh's auto-PR intake, `archive_cards.py`) don't apply there at all. CARD-0315 (the open cross-repo protocol question) stays open. |
| 1.13 | CARD-0306 — updated the reference to the general Session Start list, which moved from being inlined in root `CLAUDE.md` to its own `tos/JCTsh-Session-Start.md` (same read-frequency-split principle this document's own history already applies to itself). The 9-step numbering this document's table maps against is unchanged. |
| 1.12 | Reconciled the Component/Cluster Registry against `kanban-board.md`'s actual tags (CARD-0294's closing step, Joseph: "reconcile the list of tags to the clusters in the component session start up") — added a new `architecture` row (the directory CARD-0294 itself created had no cluster yet), confirmed every real-directory tag maps to exactly one existing row, and documented three deliberate non-matches: planned-component tags with no directory yet, the non-project `personal` tag, and multi-directory clusters using per-directory tags rather than one unified cluster tag. |
| 1.11 | Added "one component, one owning cluster" (CARD-0299 follow-on, Joseph asking directly about `air-quality-monitor`'s dual relevance to both `hiking-monitor cluster` and hike-izer's data pipeline) — a component is never listed under two clusters; another domain's work reaches it by reading its files directly, not by co-owning it. Same single-source-of-truth principle already applied to documentation, applied here to component ownership. |
| 1.10 | Renamed Session Roster to Component/Cluster Registry and predefined every remaining component/core/host into a cluster (CARD-0299 follow-on, Joseph: "this table identifies the clusters" / "add all the clusters... so they are predefined") — 11 new proposed clusters, none yet initiated, covering every directory not already in one of the three live clusters. |
| 1.9 | Session Roster gained an "Initiated" column (CARD-0299 follow-on, Joseph) — distinguishes a real, resumable session from a recorded-but-not-yet-started candidate scope, so the roster can hold a recommended cluster before anyone actually starts it. |
| 1.8 | Added a Session Roster (CARD-0299) — which component/cluster sessions exist and what each covers, kept current by a reconciliation check added to step 3 of the initiation procedure. Revised step 2's "no separate registry or naming file is needed" line, which a third (deliberately initiated) instance showed no longer held. |
| 1.7 | Split this changelog out to this file (CARD-0292) — proactive, alongside the analogous `JCTsh-Operating-System.md` split, before this document's own chain grew as large. From this version on, the live document's header holds only the current version's description; this file is the complete record. |
| 1.6 | Corrected the document's own framing (CARD-0284 follow-on, Joseph) — a component session does not run every general Session Start step unmodified and then add more on top, as previously stated. Replaced that framing with a per-step table covering all 9 general steps (scoped / skipped / unscoped, decided individually), so the two step-lists below are now genuinely "instead of," not "in addition to." |
| 1.5 | Added step 5 (CARD-0282) — scope `CLAUDE.md`'s `/status` device-health check to this session's own covered component(s) only, the same scoping principle step 3 already applies to kanban checks, applied here to device/dashboard health instead. |
| 1.4 | Step 4 revised (CARD-0284 follow-on) — softened "resume it, never restart it" to "default to resuming," with a real case for a periodic deliberate restart (context/cost bloat, self-testing that the `.md` files are actually sufficient on their own, shedding stale intermediate reasoning) — not just a fallback for a lost session. Also noted the mechanical point that resuming works just as well from a clean shell (`claude --resume`/`--continue`) as from `/resume` inside another session, which is the practical way to run two component sessions side by side. |
| 1.3 | Added "Initiating or Rebuilding a Component Session" (CARD-0284, moved to Build) — generalizes the two real instances (hike-izer, `tos`) into a repeatable 4-step procedure for starting a new component/cluster session or reconstructing an existing one, using nothing beyond this document's own existing startup steps. |
| 1.2 | Step 3 extended (CARD-0284 follow-on) — the component-tag scoping isn't just for the automated startup kanban checks, it's the default for any card-related request made during the session (e.g. "list the open cards" defaults to this session's own component tag(s), not the whole board, unless Joseph asks otherwise). Found live: a `tos`-component-session "list the open cards" request was answered whole-board instead, because the prior wording only covered the automatic sweep. |
| 1.1 | Step 2 updated (CARD-0290) — `CLAUDE.md` is genuinely readable in full now that archived card history lives in a dedicated `card-archive.md` sibling instead of being appended directly into `CLAUDE.md` (which had grown as large as 485KB before the split). `card-archive.md` explicitly stays out of this list — on-demand only, never routine reading. |
| 1.0 | Initial release (CARD-0284) — original 4-step startup list, description not separately recorded before the chaining convention began. |

**Related:** `tos/kanban-board.md` CARD-0292 (this split), `JCTsh-Operating-System-History.md` (the analogous split for that document).
